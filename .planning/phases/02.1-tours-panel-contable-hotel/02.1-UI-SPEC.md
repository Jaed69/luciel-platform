---
phase: 02.1
slug: tours-panel-contable-hotel
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-04
---

# Phase 02.1 — UI Design Contract

> Visual and interaction contract for the Tours contable panel — an internal mini-ERP
> (B2B, multi-user, double-entry) replacing the hotel/agencia's Excel+VBA macro sheet.
> Inherits the **Basílica** design DNA verbatim (`.planning/design-systems/basilica.md`) and adapts it
> to a dense accounting surface per the locked "Adaptación para Panel Tours" section of that file.

**Sources pre-populated (do not re-ask):**
- `.planning/design-systems/basilica.md` — full token tables, button/card/nav/input specs, Do's/Don'ts, and the Panel Tours adaptation block at the bottom.
- `02.1-CONTEXT.md` — D-01..D-26 (locked topology, multi-moneda rule, comisión model, audit_log, RBAC).
- `02.1-RESEARCH.md` — stack (Next.js 16 + RSC + Server Actions / FastAPI + SQLAlchemy async + aiosqlite / NextAuth credentials + JWT bridge), project structure, Architecture Responsibility Map.
- `02.1-VALIDATION.md` — verification map drives which screens must exist.
- `.planning/REQUIREMENTS.md` TOURS-01..10, `.planning/ROADMAP.md` SC#1–10.

**Stack lock relevant to UI:**
- Frontend: Next.js 16.2.10 App Router + RSC + Server Actions + TypeScript 5.8 + Tailwind v4.
- UI library: **NO external component library.** Local-only `apps/tours/web/src/components/` primitives built on Tailwind 4. (shadcn = none; Radix/MUI/Chakra = none — out of scope per STATE.md deferred `packages/ui`).
- Server-side data: RSC fetch to `http://tours-api:8000` with `Authorization: Bearer <JWT>` (D-02). No client→API calls except via Server Actions / Route Handlers.
- No PWA, no charts, no Excel/PDF export (deferred to 2.1.x).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (local Tailwind 4 primitives in `apps/tours/web/src/components/`) |
| Preset | not applicable |
| Component library | none — strictly local primitives on Tailwind v4 (`@tailwindcss/vite`) |
| Icon library | Phosphor Icons (`@phosphor-icons/react` — only if aLive needs icons; otherwise inline SVG). Start with inline SVG for nav + status badges; add Phosphor only when ≥3 icons repeat. |
| Font — display / headings | **Playfair Display** (700/600). Loaded via `next/font/google` in root layout, `--font-playfair` CSS var. |
| Font — body / forms / tables | **Nunito Sans** (400/600). `next/font/google`, `--font-nunito` CSS var. |
| Font — script accent | **Yeseva One** (400). `--font-yeseva`. **Only** on login hero + brand stamp in nav. Forbidden in tables/forms/nav lists. |
| Language | Castellano (Perú). All UI copy in Spanish; code identifiers in English. |
| Date format | UI = `DD/MM/YYYY`. API = ISO `YYYY-MM-DD`. The bridge layer translates in the Server Component / API client, never in the cell renderer. |
| Currency | `S/` prefix for PEN, `$` prefix for USD. Always `tabular-nums`. Negatives in `chili-red`. |

---

## Spacing Scale

Token values verbatim from Basilica (8px base). Do not invent others.

| Token | Value | Usage in Tours panel |
|-------|-------|-------|
| `xxs` | 4px | Icon-to-text gap inside cells, inline padding chips |
| `xs` | 8px | Form input padding `12px 16px` inner (vertical=12, horizontal=16) — vertical 8 used in dense table cells |
| `sm` | 12px | Form input vertical padding, compact chip spacing |
| `md` | 16px | Default element spacing, table cell horizontal padding, dashboard card spacing |
| `lg` | 24px | Card padding (`content-card`), grid gap desktop, form fields vertical rhythm |
| `xl` | 32px | Layout gaps, form section breaks |
| `xxl` | 48px | Major section breaks, dashboard → table break |
| `section` | 80px | Page vertical padding desktop (40–56px mobile per basilica) |

**Exceptions:**
- Touch targets: tab nav items and icon-only buttons `min-h: 44px` (WCAG) even though 44 is not on the 8px scale — the visible pill stays on scale, only the hit-box overflows.
- Accounting table row height: `36–40px` (locked by basilica.md adaptation block — denser than marketing cards).
- Form layout: 2-column desktop grid gap `24px` (`lg`), collapses to 1-column mobile with `16px` (`md`) vertical gap.

---

## Typography

Three families (Playfair display / Nunito body+tables / Yeseva accent). Locked roles from basilica.md.

| Role | Font | Size | Weight | Line Height | Used For |
|------|------|------|--------|-------------|----------|
| Page heading (H1) | Playfair Display | 38px | 600 | 1.25 | Page titles ("Ventas", "Liquidaciones", "Cuentas") |
| Card heading | Playfair Display | 28px | 600 | 1.25 | Saldo number in dashboard card, card titles |
| Section subhead (H3) | Playfair Display | 24px | 600 | 1.25 | Card section labels, modal titles |
| Body | Nunito Sans | 16px | 400 | 1.50 | Help text, modal paragraphs, empty-state body |
| Body large | Nunito Sans | 19px | 400 | 1.60 | Login hero body only |
| Button | Nunito Sans | 16px | 600 | 1.40 | All buttons |
| Small | Nunito Sans | 14px | 600 | 1.50 | Table column headers, field labels uppercase-tracked, badges |
| Micro | Nunito Sans | 13px | 400 | 1.50 | Table cells, captions, metadata, audit-log rows |
| Script accent | Yeseva One | 40px | 400 | 1.30 | Login hero only ("Tours") — max 1–2 words |
| Code / IDs | Nunito Sans tabular-nums | 13–14px | 400 | 1.50 | `LIQ-2026-001`, account codes, currency amounts |

**Rules inherited:**
- Body never pure black → use `rgba(46,21,18,0.90)` (`text-espresso`).
- Playfair vs Nunito never mixed inside a single text block.
- Yeseva One only on login brand stamp. Zero Yeseva in dashboard / tables / forms / nav.
- Per basilica.md Panel Tours adaptation: **dense accounting tables use Nunito 13–14px** (Micro / Small roles above) — not the marketing 16px body.

---

## Color

Verbatim from basilica.md. Panel Tours adaptation reassigns "dominant" and "secondary" surfaces to ERP-appropriate canvas zones, preserving the warm wine/cream DNA.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `peach-cream #F7E9D3` | Page canvas for all authed app routes (dashboard, ventas, liquidaciones, catálogos, admin). Login hero canvas. |
| Dominant alt | `canvas #FFFFFF` | `content-card` surfaces, table body rows even, modal surfaces, form fields after focus-valid tint |
| Secondary (30%) | `stone-wall #EDE1CC` | Table zebrado at 30% opacity, dashboard section washes, zone separators, filter bar |
| Secondary dark | `espresso-wine #2E1512` | Sidebar nav (if used) / footer band / feature-band on dark routes (audit-log focus strip) — moderated vs marketing use |
| Accent (10%) | `primary #6B1E1C` (Wine) | Pill CTAs filled, page headings, table header text, sticky-table-header Wine+Gold bottom hairline, status badge "cerrada" (muted variant `wine-muted #4A2320`), focus border of `floating-label-input` |
| Accent trim | `gold #C9A15A` | Hairlines on cards and tables, `button-gold-trim` border, `button-outlined` border, ceremony seal-badge ring on login, sticky table header bottom rule. **Never as flat fill.** |
| Semantic — destructive | `chili-red #9C2B2B` | Delete confirmations, error text, toast left-border, negative currency amounts, validation message text, "cerrar liquidación" destructive action |
| Semantic — valid | `wine-light-tint hsl(4 55% 30% / 8%)` | Valid form field background tint |
| Semantic — invalid | `chili-tint hsl(4 62% 40% / 6%)` | Invalid form field background tint |
| Semantic — warning | `amber-warning #E0A83E` | Status badge "abierta" (in liquidaciones list), pre-check warning rows in close-liquidación modal |
| Skeleton shimmer | `gold-light #DFC08A` → `stone-wall #EDE1CC` | Loading skeleton shimmer (no cool gray spinners) |
| Text on light | `text-espresso rgba(46,21,18,0.90)` | Body, headings, table cells on light surfaces |
| Text on light soft | `text-espresso-soft rgba(46,21,18,0.62)` | Captions, metadata, audit-log timestamps, empty-state body |
| Text on dark | `on-dark #FFFFFF` | Sidebar / footer / feature-band text |
| Text on dark soft | `on-dark-soft rgba(255,255,255,0.72)` | Metadata on dark bands |
| Text on Wine | `on-primary #FFFFFF` | Pill button text, Wine badge text |

**Accent reserved for (explicit list — never "all interactive elements"):**
- Pill CTAs filled (the single dominante action per page).
- Page-heading H1 text color.
- Table header text color and its Gold bottom hairline.
- Focus border of `floating-label-input`.
- Status badge "cerrada" (wine-muted fill, white text).
- Cached "filas Egreso" / Sandra-equivalent totals emphasis in close-liquidación modal.

**Destructive reserved for (explicit list):**
- Delete actions (catálogo rows, tours_servicios unlocked, usuarios except self/last-admin restrictions).
- Close liquidación confirmation button (irreversible-ish without reabrir).
- Reabrir liquidación (reverses asientos paired).
- Validation-error text and invalid-field tint.
- Toast left border for error notifications.
- Negative currency amounts.

---

## Copywriting Contract

Spanish (Perú) throughout. Proposing strings — checker validates. Verb+noun CTAs locked.

| Element | Copy | Notes |
|---------|------|-------|
| **Login — primary CTA** | `Iniciar sesión` | `button-primary` Wine pill |
| **Login — script brand stamp** | `Tours` | Yeseva One 40px, gold color, login hero only |
| **Login — error toast** | `Correo o contraseña incorrectos. Verifica tus datos e inténtalo de nuevo.` | chili-red left border, top toast |
| **Login — email label** | `Correo` | floating-label-input |
| **Login — password label** | `Contraseña` | floating-label-input |
| **Dashboard — page heading** | `Resumen contable` | H1 Playfair 38px |
| **Dashboard — empty** heading | `Sin movimientos para este filtro` | Playfair 24px |
| **Dashboard — empty body** | `No hay asientos en el rango de fechas seleccionado. Cambia los filtros o registra tu primera venta en "Ventas".` | Nunito 16 body-espresso-soft + `button-outlined` "Ir a Ventas" |
| **Dashboard — filter button** | `Aplicar filtros` | button-primary pill |
| **Dashboard — reset** | `Limpiar filtros` | button-outlined |
| **Ventas — primary CTA** | `Registrar venta` | button-primary Wine pill |
| **Ventas — comisión preview label** | `Comisión estimada` | above the preview number, small 14px Nunito 600 |
| **Ventas — preview empty/help** | `Completa tour, vendedor y monto para previsualizar la comisión.` | Nunito 14 small, espresso-soft |
| **Ventas — preview error** | `No se pudo resolver una regla de comisión. Configura una regla en "Catálogos → Comisiones" o usa la regla global por defecto.` | chili-red text |
| **Ventas — success toast** | `Venta registrada. Asiento {asiento_id} cuadrado.` | top toast, wine-light-tint left border |
| **Ventas — cancel** | `Cancelar` | button-outlined |
| **Ventas — empty state (no tours catálogo)** | `No hay tours cargados. Agrega uno en "Catálogos → Tours" antes de registrar una venta.` | Nunito 16 body-espresso-soft + `button-outlined` "Ir a Tours" |
| **Liquidaciones — page heading** | `Liquidaciones` | H1 |
| **Liquidaciones — list empty** | `Todavía no hay liquidaciones. Crea la primera con "Nueva liquidación".` | + button-primary "Nueva liquidación" |
| **Liquidaciones — open CTA** | `Nueva liquidación` | button-primary (admin / contabilidad only; hidden from vendedor) |
| **Liquidaciones — close CTA** | `Cerrar liquidación` | button-primary on detail page; destructive modal confirmation follows |
| **Liquidaciones — close modal title** | `Cerrar liquidación LIQ-AAAA-NNN` | Playfair 24px modal title |
| **Liquidaciones — close modal preview** | `Se generarán los siguientes asientos de comisión:` | then a table: vendedor · margen · % regla · comisión · débito 501 · crédito 201 |
| **Liquidaciones — close modal confirmation copy** | `Confirmar cierre` | button-primary. Warn: `Esta acción no se puede deshacer directamente. Para ajustar, usa "Reabrir" (genera asientos de reversión).` (Nunito 14, espresso-soft, with chili-red warning icon) |
| **Liquidaciones — close pre-check failure** | `No se puede cerrar la liquidación:` (then bullet list of failing tours/rules). | chili-red text + list; modal stays open; `button-primary` disabled |
| **Liquidaciones — reopen CTA** | `Reabrir liquidación` | button-outlined (admin / contabilidad only) |
| **Liquidaciones — reopen confirmation** | `Reabrir LIQ-AAAA-NNN` / body: `Se generarán asientos de reversión para cancelar el cierre original. La liquidación original queda en estado "revertida" (visible en auditoría), los tours del rango se desbloquean para edición, y al volver a cerrar se creará una nueva instancia.` | modal |
| **Liquidaciones — status badges** | `abierta` / `cerrada` / `revertida` | abierta = amber-warning; cerrada = wine-muted; revertida = dusty-mauve text on stone-wall pill |
| **Liquidaciones — tour locked hint** | `Tour en liquidación cerrada. Reabre la liquidación para editar.` | Nunito 13 micro, chili-red |
| **Catálogos — primary CTA (per catálogo)** | `Agregar {entidad}` (e.g. `Agregar agencia`, `Agregar tour`, `Agregar vendedor`, `Agregar forma de pago`, `Agregar moneda`, `Agregar regla de comisión`) | button-primary |
| **Catálogos — save** | `Guardar {entidad}` (e.g. `Guardar agencia`, `Guardar tour`, `Guardar vendedor`, `Guardar forma de pago`, `Guardar moneda`, `Guardar regla de comisión`) | button-primary in modal footer |
| **Catálogos — cancel modal** | `Cancelar` | button-outlined |
| **Catálogos — edit** | `Editar` | text-link button, Wine text |
| **Catálogos — delete confirm (soft delete)** | `Eliminar {nombre}` / body: `El registro se marcará como inactivo. Aparece atenuado en la lista. Para restaurar, usa "Restaurar".` | destructive modal, chili-red "Sí, eliminar" button + button-outlined "Cancelar" |
| **Catálogos — global rule delete guard** | `Eliminar regla de comisión` / body: `Esta es la regla global por defecto (50/50). No se puede eliminar — solo editar el porcentaje o el alcance.` | destructive modal but **delete disabled**, only "Cerrar" + "Editar %". |
| **Catálogos — empty** | `No hay {entidades} cargadas todavía.` | body-espresso-soft + `button-primary Agregar {entidad}` |
| **Audit log — page heading** | `Auditoría` | H1 (admin only) |
| **Audit log — empty filter** | `No hay eventos que coincidan con los filtros.` | body-espresso-soft |
| **Audit log — JSONB row toggle** | `{Ver datos}` / `{Ocultar datos}` | text-link, Wine; JSONB pretty-printed in monospace Nunito 13 inside a stone-wall panel |
| **Usuarios — page heading** | `Usuarios` | H1 (admin only) |
| **Usuarios — primary CTA** | `Nuevo usuario` | button-primary |
| **Usuarios — reset password** | `Restablecer contraseña` | text-link per row → modal |
| **Usuarios — delete self guard** | `Eliminar usuario` / body: `No puedes eliminar tu propia cuenta ni al último administrador.` (rule: button disabled on self row and last admin row, hover micro-tooltip "Último admin o cuenta propia") |
| **Global — 403 RBAC message** | `No tienes permiso para acceder a esta sección.` | Playfair 24px + Nunito 16 body-espresso-soft, on peach-cream empty state, no nav highlight |
| **Global — server 500 toast** | `Error del servidor. Reintenta en unos segundos o contacta al administrador.` | top toast, chili-red left border |
| **Global — session expired** | `Tu sesión expiró. Vuelve a iniciar sesión.` | then redirect to /login |
| **Global — footer minor** | `© {YYYY} Tours Panel · Cusco, Perú` | Nunito 13 micro, espresso-soft, espresso-wine band footer |

**Destructive actions table (per basilica.md "must declare"):**

| Action | Where | Confirmation | Notes |
|--------|-------|--------------|-------|
| Soft-delete catálogo row | Catálogos admin | Modal: `Eliminar {nombre}` + warning body, chili-red "Sí, eliminar" + "Cancelar" | Soft delete (estado=inactivo). Restorable. |
| Delete `comision_reglas` global | Catálogos → Comisiones | **Disabled** — modal informs rule not deletable, only editable | D-10 lock. |
| Delete `usuarios` (self or last admin) | Usuarios | **Disabled** — micro-tooltip on hover | "Cannot delete self or last admin." |
| Close liquidación (irreversible-ish) | Liquidaciones detail | Modal with asientos preview table + warn copy + Confirmar cierre | D-11/D-13 — reabrir is the reversal path, not inline edit. |
| Reabrir liquidación (reversal asientos) | Liquidaciones detail (estado=cerrada) | Modal explaining reversal asientos + estado revertida | D-13 lock. |
| Delete `tours_servicios` unlocked row | Ventas | Modal: `Eliminar venta {id}` + warn that asiento reversal will be generated + `Confirmar` / `Cancelar` | Avoids orphaned asientos. Row must NOT be in a closed liquidación (D-14). |
| Logout | Nav / user menu | None (immediate, non-destructive) | — |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — stack locked to local Tailwind primitives | not applicable — no shadcn, no `components.json` |
| Radix UI | none | not applicable |
| Material UI / Chakra / Mantine | none | not applicable |
| Third-party registries | none | not applicable — CONTEXT.md D-02 + STATE.md defer `packages/ui` shared; tours builds primitives locally |

**No external component library enters the contract. No npm registry install, no `shadcn view` step required, no third-party block vetting necessary.** All UI primitives live in `apps/tours/web/src/components/` as local Tailwind v4 className compositions (e.g. `Button.tsx`, `ContentCard.tsx`, `FloatingLabelInput.tsx`, `DataTable.tsx`, `StatusBadge.tsx`, `Modal.tsx`, `Skeleton.tsx`, `Toast.tsx`). No `components.json` will be generated.

---

## Screen-Level Interaction Contracts

The template is a floor, not a ceiling. The following screen contracts give the executor enough specificity to implement without design ambiguity and let the checker validate per-screen interactions.

### S1 — Login (`/login`)

**Layout:**
- Peach-cream full-bleed hero. Centered single column max-w `420px`.
- `arch-niche-divider` at top — amber-glow radial behind arch — used as brand anchor only here (not on authed routes).
- Yeseva One 40px gold "Tours" script accent centered above arch; Playfair 38px "Iniciar sesión" H1 below.
- Form below: `floating-label-input` for `Correo` and `Contraseña`, stacked vertical with `lg (24px)` spacing.
- `button-primary` "Iniciar sesión" Wine pill full-width.
- Helper below: "¿Olvidaste tu contraseña? Contacta al administrador." (Nunito 14 small, espresso-soft).

**Interaction:**
- Submit → Server Action → NextAuth Credentials authorize → FastAPI `/auth/login` → JWT issued.
- **Role-aware redirect on success:** admin → `/`, vendedor → `/ventas`, contabilidad → `/` (dashboard). Determined from JWT `role` claim.
- On failure: chili-red toast top bar with copy above; field bg turns chili-tint; inputs remain.
- Skeleton: none (login is fast). Loading state on button: Nunito 14 white "Iniciando…" + `opacity-90` lock.

**RBAC:** unauthenticated route. All authed users redirected away. No nav shown.

---

### S2 — Dashboard mínimo (`/`)

**Layout:**
- Peach-cream canvas.
- H1 "Resumen contable" Playfair 38px at top with `lg (24px)` margin-bottom.
- **Filter bar** card (`content-card`) full-width: fields in 2-column desktop / 1-column mobile:
  - `Desde` (date picker native, DD/MM/YYYY — wire to `date` input formatted)
  - `Hasta` (same)
  - `Agencia` (select, populated from `/agencias`)
  - `Vendedor` (select, populated from `/vendedores`)
  - `Moneda` (select: PEN / USD / Ambas)
  - Footer: `button-primary` "Aplicar filtros" + `button-outlined` "Limpiar filtros"
- **Saldo por cuenta grid:** 4 `content-card`s in a 2x2 desktop / 1x4 mobile grid (grid gap `lg 24px`):
  - "Caja Soles (S/)"
  - "Caja Dólares ($)"
  - "Ingresos Tours"
  - "Costos Tours"
  - Each card: Nunito 14 small label top-left, Playfair 28px saldo number bottom, `tabular-nums`. Negative → `chili-red`. Positive → `text-espresso`. Card heading style. Wine hairline + Gold bottom inside accent for emphasis.
- **Filterable table** (`content-card`) under the grid: columns `Cuenta · Moneda · Débito · Haber · Saldo`. Sticky header (Wine text + Gold bottom hairline), row height 36–40px, Nunito 13 micro cells, zebrado `stone-wall` at 30% opacity. `lg` max-h so sticky works on scroll.
- **Tours pendientes de liquidación** table at bottom — column "Días desde venta" rises in chili-red强度 past 30/60/90 day thresholds (no scheduler, just visual). Sorted by fecha asc (oldest first). (D-20.)

**Interaction:**
- Filter form submit → Server Component re-fetch with `?fecha_desde&fecha_hasta&agencia_id&vendedor_id&moneda` → Server Action → FastAPI `/dashboard/saldos` + `/dashboard/tours_pendientes`.
- Loading state: skeleton shimmer warm (`gold-light` → `stone-wall`) on cards + table body. No cool spinner.
- Empty state: copy above.
- **No charts.** Ever. (Deferred to 2.1.x.)

**RBAC:**
- admin: sees all 4 cards + global totals + tours pendientes.
- contabilidad: same as admin (without access to Usuarios route).
- **vendedor: sees ONLY their own ventas + own liquidaciones.** Cannot land on `/` dashboard — redirect to `/ventas` from nav and route guard. If vendedor navigates to `/`, return 403 page with copy above. Nav hides "Resumen contable" and "Auditoría" + "Usuarios".

---

### S3 — Ventas (`/ventas`)

**Layout:**
- H1 "Ventas" Playfair 38.
- Top action bar: `button-primary` "Registrar venta" (admin + vendedor + contabilidad all may register; vendedor only sees own period vs all-period toggle hidden).
- **List table** below: columns `Fecha · Tour · Vendedor · Agencia · Forma de pago · Moneda · Monto · Comisión estimada · Liquidación · Acciones`. Sticky header. Row 36–40px. Currency column tabular-nums + `S/`/`$` prefix. Liquidación column shows `LIQ-2026-NNN` if assigned, else "—" in `text-espresso-soft`.
- "Acciones" cell: text-links "Editar" (Wine) and "Eliminar" (chili-red). If row locked (assigned to closed liquidación): both replaced by micro "Tour en liquidación cerrada. Reabre la liquidación para editar." (Nunito 13, chili-red), no links.
- Filters: fecha range, agencia, vendedor, tour, moneda. Same filter-bar pattern as dashboard.
- Pagination pill bottom-center (page numbers as pills; current page Wine fill).
- **"Registrar venta" → modal** (content-card max-w `640px`):
  - 2-column desktop / 1-column mobile form, gap `lg 24px`:
    - Tour (select — from `tours_catalogo`)
    - Vendedor (select — from `vendedores`; vendedor user defaults to self, disabled)
    - Agencia (select)
    - Forma de pago (select)
    - Moneda (select: PEN / USD — must match selected tour's priced moneda; warn if mismatch)
    - Monto (number, currency-formatted with S/ or $ dynamic prefix)
    - Costo proveedor (number) — optional at registration but required before liquidación close (D-17 pre-check)
    - Fecha (date, DD/MM/YYYY)
    - Notas internas (optional textarea, part of `metadata` JSONB)
  - **Live comisión preview** above footer: after tour + vendedor + monto filled, a Server Action calls `/simular` and renders "Comisión estimada: S/ 123.45 (regla: vendedor+tour, 12%)" — invalidated when fields change. Skeleton shimmer warm while fetching. Error copy above if rule unresolved.
  - Footer: `button-primary` "Registrar venta" + `button-outlined` "Cancelar".
  - Submit → Server Action → FastAPI POST `/ventas` → inserts `tours_servicios` + posts balanced asiento in single tx (D-15). On success: toast copy above, table refreshes, modal closes.

**Interaction notes:**
- NO free-text in tour/vendedor/agencia/forma_pago/moneda picks — selects from catálogos only (SC#8).
- Monto formatted as currency as the user types (tabular-nums).
- Form layout tight 2-col with `lg` gap. Mobile collapses to single col with `md` gap.

**RBAC:**
- admin: all ventas, all filters.
- vendedor: only own ventas (filter auto-applied, vendedor select disabled=own).
- contabilidad: all ventas, read+write.

---

### S4 — Liquidaciones (`/liquidaciones`, `/liquidaciones/[id]`)

**List:**
- H1 "Liquidaciones" + `button-primary` "Nueva liquidación" (admin + contabilidad only; vendedor sees list filtered to own and no open CTA).
- Table: `Código · Estado · Rango · Vendedor/as (si filtro) · Tours · Margen total · Comisión total · Acciones`. Status badges per copy table. Pagination pill.
- Filters: estado, fecha_desde/hasta, vendedor.

**Detail (`/liquidaciones/[id]`):**
- Header: `LIQ-AAAA-NNN` (Playfair 28px card-heading) + status badge + rango (`DD/MM/YYYY → DD/MM/YYYY`).
- Pre-check summary strip (only when estado=abierta): list of warnings (tours sin costo, reglas faltantes, TC inland pendientes) — Nunito 13 micro, amber-warning icon for warnings, chili-red for blockers (D-17).
- Tours table inside liquidación: same row spec as Ventas. Already-locked rows show the chili-red hint.
- Footer actions:
  - estado=abierta (admin + contabilidad): `button-primary` "Cerrar liquidación" → opens close modal.
  - estado=cerrada (admin + contabilidad): `button-outlined` "Reabrir liquidación" → opens reopen modal.
  - vendedor (own liquidación view): no CTA, read-only.

**Close modal:**
- Title: per copy table.
- Body: explanation + the asientos preview table (vendedor · margen · comisión % · comisión S/ · asiento line: débito 501-COSTOS-COMISIONES, crédito 201-COMISIONES-POR-PAGAR). Multi-vendedor rows.
- Warn copy (chili-red icon): irreversibility + reabrir path.
- Pre-check failures (if any): chili-red block lists specific tours (e.g. `T-100 sin costo, T-104 sin regla`); `button-primary` disabled.
- Confirm: `button-primary` "Confirmar cierre" + `button-outlined` "Cancelar".
- On success: toast "Liquidación LIQ-2026-001 cerrada. {N} asientos generados." + redirect back to list, detail shows estado=cerrada.

**Reopen modal:**
- Title + body explaining reversal asientos path (per copy table) + wine-soft-warning icon.
- Confirm: `button-primary` "Reabrir liquidación" + `button-outlined` "Cancelar".
- On success: toast "Liquidación LIQ-2026-001 reabierta. Asientos de reversión generados. Estado actual: abierta (nueva instancia LIQ-2026-002)."

---

### S5 — Catálogos admin CRUD (`/catalogos/agencias`, `/catalogos/tours`, `/catalogos/vendedores`, `/catalogos/formas-pago`, `/catalogos/monedas`, `/catalogos/comisiones`)

- Sub-nav: tab bar pills `Agencias · Tours · Vendedores · Formas de pago · Monedas · Comisiones` (Nunito 600 14, active pill Wine fill).
- Admin-only route group. Hidden from vendedor + contabilidad nav.
- Each tab is one CRUD list:
  - H1: e.g. "Agencias" Playfair 38 + `button-primary` "Agregar agencia".
  - Table: column set per entity. Sticky header. Row 36–40. Soft-deleted rows show atenuados (opacity 60%) with `Restaurar` text link.
  - Inline edit cell (click row → opens modal `content-card` max-w `md`).
- **Comisiones tab:**
  - Table: `Vendedor · Tour · Porcentaje (%) · Origen (vendedor+tour / vendedor / tour / global) · Editar · Eliminar`.
  - The row with `vendedor=NULL & tour=NULL` (global default) has Editar enabled but Eliminar **disabled** with micro-tooltip "Regla global por defecto — no eliminable". (D-10.)
  - "Agregar regla" modal: vendedor (optional select), tour (optional select), porcentaje (number). Visualizes which precedence slot it will land in before save ("Esta regla se aplicará cuando vendedor=X y tour=Y").
- Empty states per copy table.
- Delete confirmation per destructive table.

---

### S6 — Audit log viewer (`/admin/auditoria`)

- H1 "Auditoría" Playfair 38 (admin-only — RBAC D-24).
- Filter bar: `Usuario` (select), `Tabla` (select: populated from audited tables list), `Acción` (select: INSERT/UPDATE/DELETE), `Fecha desde`, `Fecha hasta`. `button-primary` "Filtrar" + `button-outlined` "Limpiar".
- Table dense (row 36–40, Nunito 13 micro cells): `Usuario · Tabla · Registro ID · Acción · Fecha · Datos`. Acción column: INSERT=green-ish wine-light-tint pill "I", UPDATE=amber-warning pill "U", DELETE=chili-red pill "D" (text only).
- **Datos column:** two-row cell — top "Antes:" toggle text-link "Ver datos" (Wine), bottom "Después:" same. Click expands pretty-printed JSONB inside a `stone-wall` panel (monospace Nunito 13). Sensitive fields show `"password_hash": null` (D-26).
- Pagination pill.
- Empty state per copy table.

---

### S7 — Usuarios admin (`/admin/usuarios`)

- H1 "Usuarios" Playfair 38 (admin-only).
- `button-primary` "Nuevo usuario".
- Table: `Correo · Nombre · Rol · Estado · Último acceso · Acciones`. Rol column shows pill: admin (Wine), contabilidad (gold-bordered), vendedor (stone-wall bordered).
- Actions per row: text-links "Editar" (Wine) and "Restablecer contraseña" (Wine). "Eliminar" text-link (chili-red) — **disabled** on self row and on last admin row with micro-tooltip copy above (rule preserved: cannot delete self or last admin).
- **Nuevo usuario modal** (content-card max-w `md`): 2-col form — Correo, Nombre, Rol (select admin/vendedor/contabilidad), Estado (activo/inactivo), with first-password set by admin (Nunito 13 micro helper "Contraseña inicial será hasheada con bcrypt; el usuario debe cambiarla al primer ingreso" — even if not implemented as forced-change in 2.1, the helper surfaces the expectation). Footer `button-primary` "Crear usuario" + `button-outlined` "Cancelar".
- **Restablecer contraseña modal** (content-card max-w `sm`): Nueva contraseña + Confirmar. Footer `button-primary` "Restablecer" + `button-outlined` "Cancelar".

---

## Global Patterns & State Specs

### Nav (Persistent across authed routes)
- `global-nav` spec from basilica.md. Peach-cream bg, Wine text links Nunito 600 14, crests logo izq (`seal-badge` mini 40px with "Tours" stamp), nav center, user pill + logout right.
- **RBAC-driven item hiding (not just route-blocking — basilica.md adaptation lock):**

| Item | admin | contabilidad | vendedor |
|------|-------|--------------|----------|
| Resumen contable (/) | visible | visible | **hidden** (redirect → /ventas) |
| Ventas | visible | visible | visible (own only) |
| Liquidaciones | visible | visible | visible (own only) |
| Catálogos | visible | **hidden** | **hidden** |
| Auditoría | visible | **hidden** | **hidden** |
| Usuarios | visible | **hidden** | **hidden** |
| User menu | visible (name + rol) | visible | visible |
| Logout | visible | visible | visible |

### Toastsystem
- Top of viewport fixed bar, height 48px, max-w `640px` centered, `content-card` shadow treatment warm.
- Success → `wine-light-tint` left border (4px), Nunito 14 600 text-espresso.
- Error/destructive → `chili-red` left border.
- Warning (pre-check failures而非 fatal) → `amber-warning` left border.
- Auto-dismiss 5s on success, 8s error (sticky with "X" close).
- One toast at a time; new replaces old with quick fade.

### Skeletons (Loading)
- Warm shimmer `gold-light #DFC08A` → `stone-wall #EDE1CC`. Never cool gray.
- Card skeleton = `content-card` outline + 3 shimmer lines.
- Table skeleton = sticky header real + N rows of shimmer cell-rows.

### Empty states
- peach-cream canvas, centered content-card max-w `md`.
- Playfair 24px heading + Nunito 16 body-espresso-soft body + single `button-outlined` "Ir a {next-step route}".
- Used for: dashboard no-movimientos, liquidaciones list empty, catálogos empty, audit filter no-result.

### Modals
- `content-card` max-w by content (`sm` = 480px, `md` = 640px, `lg` = 768px).
- Warm shadow per basilica.
- Backdrop = `espresso-wine` at 50% opacity (warm warm dark, not cool gray).
- Footer: `button-primary` + `button-outlined`, `lg 24px` gap, right-aligned.

### Status badges
- Pill shape, 12px radius (`sm`), Nunito 600 13, padding `4px 12px`.
- Fill/text variants:

| State | Fill | Text | Border |
|-------|------|------|--------|
| `abierta` | amber-warning tint (cream wash) | amber-warning dark | amber-warning 1px |
| `cerrada` | wine-muted | on-primary | wine-muted 1px |
| `revertida` | stone-wall | text-espresso-soft | dusty-mauve 1px |
| INSERT (audit) | wine-light-tint | primary (#6B1E1C) | none |
| UPDATE (audit) | gold-lightest | text-espresso | gold 1px |
| DELETE (audit) | chili-tint | chili-red | none |
| Active user | wine-muted | on-primary | none |
| Inactive user | stone-wall | text-espresso-soft | dusty-mauve 1px |

### Currency formatting
- Always `Intl.NumberFormat('es-PE', { style: 'currency', currency: 'PEN' })` for S/ and `currency: 'USD'` for $.
- `tabular-nums` class always applied to currency cells.
- Negative → wrap in `<span className="text-chili-red">`.

### Date formatting
- UI render: `format(date, 'dd/MM/yyyy')` (date-fns or native Intl).
- Form input: native `<input type="date">` for accessibility — mask shows DD/MM/YYYY where localized.
- Bridge layer (Server Component / api client) converts ISO ↔ DD/MM/YYYY. Never done in cell renderer.

### Validation
- `floating-label-input` valid state → field bg `wine-light-tint`, label green-ish Wine.
- Invalid → field bg `chili-tint`, label chili-red, helper text Nunito 13 chili-red under field.
- Form-level submit disables `button-primary` until all required valid.

### Error boundaries
- Next.js `error.tsx` per route group: peach-cream canvas + Playfair 24px "Error del servidor" + Nunito 16 body "Reintenta en unos segundos o contacta al administrador." + `button-outlined` "Reintentar".
- 403 page per copy table.
- 404: Playfair 24 "Página no encontrada" + Nunito 16 "La URL no existe." + `button-outlined` "Volver al inicio".

---

## Accessibility Floor (non-negotiable)

- All interactive elements keyboard-reachable; focus-visible ring uses `wine` 2px offset 2px.
- Tables: use real `<table>` with `<thead>/<tbody>`, sticky header is `position: sticky; top: 0`.
- Status badges and icon-only actions → `aria-label` in Spanish (e.g. `aria-label="Eliminar agencia"`, `aria-label="Cerrar liquidación"`).
- Toasts: `role="alert"` for error, `role="status"` for success.
- Tabular numbers for currency always (prevents column jitter).
- Color contrast: Wine #6B1E1C on peach-cream passes AA at heading sizes; chili-red on white passes AA at 14px+ 600. Verify with checker.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending