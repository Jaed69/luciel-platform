# Phase 02.1: Tours — Panel contable hotel - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-04
**Phase:** 02.1-tours-panel-contable-hotel
**Areas discussed:** Topología de deploy, Multi-moneda (PEN/USD), Modelo de cierre de liquidación, Audit log

---

## Topología de deploy

| Option | Description | Selected |
|--------|-------------|----------|
| 2 contenedores (Next.js standalone + FastAPI separados) | Front externo via Traefik; back interno via DNS `tours-api`. 2 imágenes GHCR. Modulares, escala natural al módulo café. | ✓ |
| 1 contenedor con ambos procesos | Una sola imagen. Más chica en compose. Acopla escalado, el módulo café no reusa backend. | |
| Front expone /api/* a backend interno via Traefik path prefix | Mismo Host (1 cert), dos routers Traefik. CORS aún requerido; backend semi-expuesto. | |

**User's choice:** 2 contenedores (recomendado).
**Notes:** Decisión de arquitectura — preserva modularidad del mini-ERP y aísla el backend de internet.

---

**Auth bridge (sub-área)**

| Option | Description | Selected |
|--------|-------------|----------|
| Server Components fetch interno + JWT | Next.js lee JWT NextAuth y envía `Authorization: Bearer`. FastAPI valida con pyjwt + `NEXTAUTH_SECRET` compartido. Sin CORS, sin cross-domain cookies. Token nunca llega al browser. | ✓ |
| Cookie de sesión cross-subdomain | Exponer `api.tours.luciel.dev` público con su cert. SameSite=Lax + credenciales. Más moving parts, backend expuesto. | |
| Service token separado + sesión duplicada | Tabla `sesiones` + sincronización. Revocable pero duplica estado. | |

**User's choice:** Server Components fetch interno.

---

**Volume SQLite**

| Option | Description | Selected |
|--------|-------------|----------|
| Volume dedicado `tours-db-data` | Cada app con su SQLite + su backup/lifecycle. Respeta PROJECT.md "apps aisladas". Módulo café futuro = su propio volume. | ✓ |
| Volume compartido `erp-data` con subdirs | Menos volumes, un backup. Pero acopla lifecycle/migración. | |

**User's choice:** Volume dedicado.

---

**CI: matrix vs workflow separado**

| Option | Description | Selected |
|--------|-------------|----------|
| Matriz por app en `release.yml` | matrix(app: [landing, tours-web, tours-api]). Escala a graph, hackathons sin tocar el workflow. Buen momento para refactor. | ✓ |
| Workflow separado `release-tours.yml` | paths filter `apps/tours/**`. Aislado pero duplica desde la 3era app. | |
| Tag scheme (combinado en matrix) | `luciel-platform-tours-web` + `luciel-platform-tours-api`. Decidido en la matrix. | |

**User's choice:** Matriz por app.

---

**Continuación (check)**

| Option | Description | Selected |
|--------|-------------|----------|
| Siguiente área | Pasar a Multi-moneda. | ✓ |
| Más preguntas | TLD expuesto, Dockerfile FastAPI, secrets. Deferred al planner. | |

**User's choice:** Siguiente área.

---

## Multi-moneda (PEN/USD)

| Option | Description | Selected |
|--------|-------------|----------|
| Una cuenta por moneda | chart separado: 101-CAJA-PEN, 101-CAJA-USD. Cuentas con `moneda` NOT NULL. Asientos single-moneda. SC#7 natural. | ✓ |
| Cuenta única + columna moneda en asiento_lineas | Asientos mixtos PEN/USD, conversión implícita. Más flexible pero diverge de SC#7. | |
| Modelo híbrido | Cuentas por moneda + `monto_original` opcional en PEN. YAGNI. | |

**User's choice:** Una cuenta por moneda.

---

**Tipo de cambio USD→PEN**

| Option | Description | Selected |
|--------|-------------|----------|
| TC capturado por asiento al registro | Admin captura TC al registrar venta. Asiento de conversión explícito entre cajas. Trazabilidad total. | |
| Tabla `tipo_cambio` diaria + conversión automática | Sincronizable con Sunat. Agrega tabla + lookup. | |
| Conversión on-demand con TC preferencial interno | El admin opera TC interno cuando necesita soles. (user free-text → evolucionó a D-06) | ✓ |

**User's free-text answer:** "Las comisiones son únicamente en soles, el cierre de las cajas es diario semanal variable, las comisiones son el resultado de lo que queda después de pagar el costo del tour a los proveedores, y el restante es lo que se considera comisión, esta se reparte cada cierto número de días, semanas o meses, aún por determinar, de lo cual el 50% de la comisión va a la empresa y el 50% se reparte a los vendedores, esta comisión es proporcional al monto total vendido."
**Follow-up (free-text):** "El distribución es margen individual del vendedor, determinado independientemente por vendedor según lo acordado con administración o perfil. La conversión de dólares a soles se realiza según necesidad, o sea si necesito soles, realizo una operación interna en la empresa de tipo de cambio preferencial."
**Final free-text:** "La comisión es tal cual según la ganancia por comisión, y esta ganancia se reparte entre empresa y vendedor en base a un porcentaje normalmente 50/50 pero eso puede variar según el acuerdo."

**Notes:** Evolución del modelo a través de 3 mensajes del usuario. Decisión final registrada en D-05..D-10.

---

## Modelo de cierre de liquidación

| Option | Description | Selected |
|--------|-------------|----------|
| Liquidación = cierre de caja de un rango | Admin abre con desde/hasta, opcional filtro agencia/vendedor. Cierra: consolida tours, calcula margen, aplica reglas, genera asientos. Reemplaza suma manual de Sandra. | ✓ |
| Por tour individual | Cada tour se cierra al terminar. Contradice reparto por período acumulado. | |
| Mensual fija | No encaja con "cierre diario o semanal variable". | |

**User's choice:** Liquidación por rango.

---

**Pago de comisión**

| Option | Description | Selected |
|--------|-------------|----------|
| Quincena 201-COMISIONES-POR-PAGAR | Cierre: débito 501 / crédito 201. Pago manual separado: débito 201 / crédito 101-CAJA-PEN. Separa evento contable de flujo de caja. | ✓ |
| Pago inmediato en el cierre | Crédito 101-CAJA-PEN directo. Contablemente weaker. | |
| Ambos modos seleccionables | Checkbox al cerrar. YAGNI — el modo por pagar + pago separado es 1 asiento más y más flexible. | |

**User's choice:** Quincena 201-por-pagar.

---

**If se agregan tours al rango post-cierre (repreguntado tras error)**

| Option | Description | Selected |
|--------|-------------|----------|
| Reabrir liquidación: revert + recalcular | Asientos de reversión + reopen. Ortodoxo, trazable. | ✓ |
| Cierre irreversible: liquidación complementaria | Nueva liquidación con rango superpuesto. Sin reversión. | |
| Edición inline sin reversión | Edita la liquidación cerrada directamente, asientos se mutan. (Respuesta inicial por error del usuario, corregida luego.) | |

**User's choice (final):** Reabrir liquidación: revert + recalcular.

---

**Tours bloqueados en liquidación cerrada**

| Option | Description | Selected |
|--------|-------------|----------|
| Bloquear edición/borrado mientras asignados a liquidación cerrada | Previene asientos inconsistentes. Para corregir, reopen. Simple, defensivo. | ✓ |
| Permite edición pero advertir | Riesgo de inconsistencia. No encaja con SC#8. | |

**User's choice:** Bloquear.

---

**Estados de liquidación**

| Option | Description | Selected |
|--------|-------------|----------|
| 3 estados: abierta → cerrada → reverted | Reabrir = reverted + nueva instancia con los tours corregidos. Previene loop de re-rev. Trazable. | ✓ |
| 4 estados: abierta → cerrada → reopened → cerrada | Permite ciclar. Mezcla trazabilidad. | |
| 2 estados + reopen in-place | Sin estado reverted. Reversión implícita al reabrir. | |

**User's choice:** 3 estados.

---

**FK asiento_id**

| Option | Description | Selected |
|--------|-------------|----------|
| asiento_id en liquidaciones (1→N) + no en tours_servicios | tours_servicios guarda su asiento de venta; liquidaciones guarda los del cierre. Separación limpia. | ✓ |
| asiento_id en tours_servicios únicamente | Liquidaciones sin FK directo; lookup por descripción. | |
| Ambas con JSONB array en liquidaciones | N asientos por cierre con JSONB. Más un campo. | |

**User's choice:** asiento_id en liquidaciones 1→N + en tours_servicios para venta.

---

**ID único visible (user free-text)**

**User free-text:** "También recuerdo que para cada cierre de caja, debe tener un id único."
**Notes:** Registrado como D-16 — `LIQ-AAAA-NNN` (año + correlativo).

---

**Operación interna TC**

| Option | Description | Selected |
|--------|-------------|----------|
| Asiento del core con `tipo=tc_interno` | Débito caja-PEN, crédito caja-USD, débito/crédito 672-GAN/PERD-TC. TC + justificación en `asientos.metadata`. Simple, no tabla extra. | ✓ |
| Tabla `tipo_cambio_interno` + asiento FK | Más estructurado,CRUD separado. TC value vive en el asiento igual. | |

**User's choice:** Asiento del core.

---

**Pre-checks al cierre**

| Option | Description | Selected |
|--------|-------------|----------|
| Sí: pre-check detiene si data incompleta | Valida costo cargado, TC necesarios, reglas resolubles. Aborta con lista. Previere liquidaciones erróneas. | ✓ |
| No: cerrar siempre, ajustar después via reopen | Menos UX pero captura errores tarde. Riesgoso para SC#8. | |

**User's choice:** Sí: pre-check.

---

**Tour sin costo cargado al cierre**

| Option | Description | Selected |
|--------|-------------|----------|
| Pre-check aborta el cierre | Muestra "Tours sin costo: T-100, T-102". Aborta hasta que se cargue. | ✓ |
| Distinguir tours abiertos vs sin costo | Permite cerrar con pendientes_costo fuera del rango. Más flexible. | |
| Cerrar siempre con costo 0 | Comisión erronea. Mal para SC#8. | |

**User's choice:** Pre-check aborta el cierre.

---

**Tour en múltiples liquidaciones**

| Option | Description | Selected |
|--------|-------------|----------|
| Un tour, una liquidación | `tours_servicios.liquidacion_id` NULL → FK a una única. Reopen si ajuste. Consistente con D-14. | ✓ |
| Tour puede participar en liquidaciones sucesivas | Tabla join + lógica anti-doble. Reapertura ya lo cubre. | |

**User's choice:** Un tour, una liquidación.

---

**Mover tour entre liquidaciones cerradas**

| Option | Description | Selected |
|--------|-------------|----------|
| No: hay que reabrir la original | Reabre original, quita tour, cierra sin ese, crea nueva con el tour. Procedimiento trazable. | ✓ |
| Sí: admin puede mover tours entre liquidaciones cerradas | Rompe bloqueo y trazabilidad. | |

**User's choice:** No (reabrir original).

---

**Tours pendientes sin cerrar por mucho tiempo**

| Option | Description | Selected |
|--------|-------------|----------|
| OK: dashboard marca tours no liquidados con días desde venta | Lista ordenada por fecha + indicador. La admin decide cuándo cerrarlos. No se fuerza. | ✓ |
| Auto-recordatorio: scheduler alerta pending >N días | Scraper/scheduler extra. YAGNI para 2.1. | |

**User's choice:** Dashboard marca tours no liquidados.

---

## Audit log

| Option | Description | Selected |
|--------|-------------|----------|
| SQLAlchemy event listener `before_flush` (async) | Captura automáticamente INSERT/UPDATE/DELETE de tablas con mixin `Auditable`. Estructural, no depende de dev acordarse. | ✓ |
| Escritura explícita en cada service method | Verboso, riesgoso (depende de familia dev). Riega el mismo problema del VBA. | |
| SQL triggers + tabla audit_log | REQUIRESMENTS prohíbe SQL triggers para balance; espíritu extensible. Sin contexto usuario_id. | |

**User's choice:** Event listener before_flush.

---

**Tablas auditadas**

| Option | Description | Selected |
|--------|-------------|----------|
| Todas las tablas con mixin `Auditable` | Cobertura total: todas las tablas de negocio + catálogos. SC#8 cumple alrededor de todo. Volumen bajo a esta escala. | ✓ |
| Sólo tablas críticas (tours_servicios, liquidaciones, etc.) | Catálogos no auditados = vector de manipulación sin rastro. | |
| Sólo lo que modifica la macro VBA actual | SC#8 específica cada INSERT/UPDATE/DELETE. Reducible. | |

**User's choice:** Todas las tablas con mixin Auditable.

---

**ContextVar + DI para usuario_id**

| Option | Description | Selected |
|--------|-------------|----------|
| `ContextVar` + FastAPI dependency injection | Setea en ContextVar al validar JWT. Event listener lee el ContextVar. Compatible con async SQLAlchemy. Patrón standard. | ✓ |
| Pasaje explícito de usuario_id al service | Verboso, fácil de olvidar. No correcto para cobertura total. | |

**User's choice:** ContextVar + DI.

---

**Permisos sobre audit_log**

| Option | Description | Selected |
|--------|-------------|----------|
| Solo `admin` | SC#2 específica "vendedor no ve audit log". Contabilidad tampoco en 2.1. | ✓ |
| admin + contabilidad | Contradice SC#2 (restrictiva). Middleton ground. | |
| Todos los roles con filtros | SC#2 específica vendedor no ve NADA de audit_log. Restringente. | |

**User's choice:** Solo admin.

---

**Schema datos_antes/datos_despues**

| Option | Description | Selected |
|--------|-------------|----------|
| Una columna TEXT por cada con JSON serializado | SQLite no tiene JSONB. json.dumps sufficiente. Simple, indexable. | ✓ |
| Columnas por campo conocido | Schema rígido, cada campo nuevo requiere migracion. | |
| JSON con diff parcial (sólo cambió) | Ahorra espacio pero complica reads del estado completo. | |

**User's choice:** Una columna TEXT por cada.

---

**Passwords en audit**

| Option | Description | Selected |
|--------|-------------|----------|
| Excluir campos sensibles (password_hash redacted) | El mixin nullifica campos sensibles antes de serializar. Auditoría registra que el cambio ocurrió, no el contenido. SC#8 seguro. | ✓ |
| Auditar todo incluidos hashes | Audit log como vector de leaks. No. | |

**User's choice:** Excluir campos sensibles.

---

## the agent's Discretion

Áreas donde el planner / researcher tienen flexibilidad:
- Dockerfile FastAPI internals (python:3.13-slim + uv + uvicorn CMD).
- Healthcheck endpoints.
- Nginx config del front (heredar de landing, ajustar para Next.js standalone output).
- Schema migrations tool (script SQL files vs Alembic — YAGNI hasta 3+ migraciones).
- Seed del chart de cuentas initial (códigos + nombres + moneda + tipo).
- Campos `metadata` JSONB híbrido en `tours_servicios`.
- UI pagination/listado + filtros dashboard (respeta SC#7).
- Implementación del endpoint `/simular` (payload shape).

## Deferred Ideas

- Full dashboard con charts + export Excel/PDF → 2.1.x
- Reportes contables completos (balance general, EERR, libro mayor) → 2.1.x
- Permisos de contabilidad sobre audit_log → 2.1.x (si operación lo pide)
- Pago de comisiones con integración bancaria automatizada → posterior
- Notificaciones automáticas + scheduler de cierre → 2.1.x
- Módulo café/Basílica → fase posterior
- `packages/ui` compartido → STATE.md deferred hasta 2+ apps
- Module registry framework → YAGNI (modulos por convención de carpetas)
- PWA de tours → 2.1.x si operación lo pide
- Migración de datos Excel → arranque limpio, pass separada post-deploy si lo necesario
- AdSense en tours.luciel.dev → tours es B2B interno, no público
- Multi-tenancy → milestone aparte
- Conciliación bancaria automatizada → fuera de alcance 2.1