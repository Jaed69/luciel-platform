---
phase: 02.1-tours-panel-contable-hotel
plan: 02
subsystem: api, ui, infra
tags: [fastapi, sqlalchemy, nextjs, sqlite, jwt, double-entry, liquidaciones, dashboard, github-actions, ghcr, matrix-ci, traefik, lets-encrypt, audit-log, rbac]

requires:
  - phase: 02.1-01
    provides: "FastAPI+SQLAlchemy core contable (post_asiento, audit listener) + commission precedence resolver + NextAuth/JWT bridge + VentaTable hint + Traefik router + GHCR pattern"
  - phase: 01
    provides: "docker-compose Traefik v3.7 + GHCR push workflow single-job landing mold"
provides:
  - "Liquidaciones state machine: POST /liquidaciones (auto-assigns tours in range), POST /liquidaciones/{id}/close, POST /liquidaciones/{id}/reopen, GET /liquidaciones/{id}/precheck — close generates balanced asientos (501 dr / 201 cr per vendedor), reopen swaps debe/haber + estado=revertida + unlock tours"
  - "LiquidacionAsientos pivot table (D-11) + codigo LIQ-AAAA-NNN incremental per year (D-16) + reopen_count counter"
  - "Pre-checks abort close if costos>0缺失 → 422 with `errors` list (D-17); TC interno pendiente is WARN-only (simplificación 2.1)"
  - "Dashboard endpoints GET /dashboard/saldos (GROUP BY cuentas with filters fecha/agencia/vendedor/moneda) + GET /dashboard/tours_pendientes (ordered asc, `dias_desde_venta` field)"
  - "RBAC backend role-forcing (T-02.1-14): vendedor JWT forces `vendedor_id = user.id` in /dashboard/saldos + /tours_pendientes (NOT UI-only) + regression test"
  - "PUT/DELETE /tours_servicios/{id} with D-14 409 lock when liquidacion.estado='cerrada'"
  - "Next.js UI: dashboard S2 (4 content-cards + filter bar + saldos table + tours pendientes with chili-red intensity), liquidaciones list + detail + CloseModal + ReopenModal (S4), admin/auditoria viewer with JSON expander + password_hash null (S6), catalogos extended with Comisiones tab (S5)"
  - "ComisionReglaModal component (S5) with precedencia slot preview"
  - "CI matrix refactor in release.yml: build/push landing+tours-web+tours-api in parallel, SSH VPS deploy pulls all 3 (D-04)"
  - "apps/tours/README.md full doc + .env.example reviewed (NEXTAUTH_SECRET generation comment explicit)"
affects: [02.2 (futuros módulos — café panel reutilizará el patrón de LiquidacionAsientos + dashboard), 03 (AdSense phase no constraints)]

tech-stack:
  added: []
  patterns:
    - "Liquidaciones state machine: abierta → cerrada (POST /close) → revertida (POST /reopen). LiquidacionAsientos pivot (cierre|reversion) links to Asientos original-id metadata for reversibility (D-11/D-13/D-19)"
    - "Per-vendedor margin commission: for each tour_servicio in range, comision_tour = (monto - costo) * (porcentaje/100) — D-09 margen not venta bruta. resolve_comision called per tour (Plan 01 service preserved as single source of precedence). One post_asiento call per vendedor to keep 501/201 atomic per asiento."
    - "LIQ-AAAA-NNN codigo generation: max(seq) WHERE codigo LIKE 'LIQ-{year}-%' + 1 — seq increments per-year only on close (not on plain insert)"
    - "GET /dashboard/saldos GROUP BY cuentas.id with `outerjoin(ToursServicios, ToursServicios.asiento_id==Asientos.id)` — accounts (e.g. 101-CAJA-PEN from TC interno asientos without tours_servicios) still appear since outerjoin"
    - "RBAC role-forcing pattern: `if user['role'] == 'vendedor': vendedor_id = int(user['id'])` BEFORE binding query params (T-02.1-14) — admin/contabilidad exempt (read-only dashboard)"
    - "TDD gate for plan 02.1-02 Tasks 1+2: RED commits (test(02.1-02): ...) precede GREEN commits (feat(02.1-02): ...) — git log --grep '^test(02.1-02)' then '^feat(02.1-02)'"
    - "@testing-library/react afterEach(cleanup) required in vitest setup.ts — without it, renders accumulate between tests and break getByText/getByTitle selectors with 'Found multiple elements'"
    - "Matrix CI strategy over app: [landing, tours-web, tours-api] with `include` providing context/file/image_suffix; tours-api matrix MUST use context=apps/tours/api (self-contained Dockerfile) — using '.' breaks the build (Plan 01 Dockerfile assumes context-relative paths for `COPY pyproject.toml` / `COPY app/`)"
    - "Deploy job: single matrix-aware SSH (NOT per-app deploys) — `docker compose pull landing tours-web tours-api` recreates all 3 services"
    - "Catalogos router extended to support `comisiones` entidad as special path rendering ComisionesTab component (separate from generic catalog CRUD) — Origen column classifies rule by (vendedor_id, tour_id) nullness — global row has Eliminar disabled with `title='Regla global por defecto — no eliminable'` per D-10"

key-files:
  created:
    - apps/tours/api/app/services/liquidaciones.py — close_liquidacion + reopen_liquidacion + get_precheck + LiquidacionPrecheckError
    - apps/tours/api/tests/test_liquidaciones.py — 5 tests
    - apps/tours/api/tests/test_comisiones.py — 3 tests (precedence, default non-deletable, simular)
    - apps/tours/api/tests/test_schema_isolation.py — 1 test (SC#9)
    - apps/tours/api/tests/test_dashboard.py — 4 tests incl T-02.1-14 regression
    - apps/tours/web/src/app/(app)/_components/DashboardCards.tsx — 4-card + filter bar reusable component
    - apps/tours/web/src/app/(app)/liquidaciones/page.tsx — S4 list
    - apps/tours/web/src/app/(app)/liquidaciones/[id]/page.tsx — S4 detail with pre-check strip + footer actions
    - apps/tours/web/src/app/(app)/liquidaciones/[id]/components/CloseModal.tsx — S4
    - apps/tours/web/src/app/(app)/liquidaciones/[id]/components/ReopenModal.tsx — S4
    - apps/tours/web/src/app/(app)/admin/auditoria/page.tsx — S6 admin-only viewer
    - apps/tours/web/src/app/(app)/admin/auditoria/_components/AuditRowExpander.tsx — Ver datos JSON toggle
    - apps/tours/web/src/app/(app)/catalogos/_components/ComisionesTab.tsx — S5 sub-component
    - apps/tours/web/src/components/comisiones/ComisionReglaModal.tsx — S5 modal
    - apps/tours/web/tests/dashboard.test.tsx — 2 tests
    - apps/tours/web/tests/catalogos.test.tsx — 3 tests
    - apps/tours/README.md — full Dev/Build/Test/Deploy/Endpoints/Decisions doc
  modified:
    - apps/tours/api/app/models/tours.py — added LiquidacionAsientos pivot table model
    - apps/tours/api/app/routers/tours.py — POST /liquidaciones auto-assign, /close|/reopen|/precheck, /dashboard/saldos | /dashboard/tours_pendientes, PUT/DELETE /tours_servicios/{id}, list_liquidaciones with filters
    - apps/tours/web/src/app/(app)/page.tsx — replaced placeholder with full S2 dashboard
    - apps/tours/web/src/app/(app)/catalogos/[entidad]/page.tsx — extended for comisiones special case + 6-tab nav
    - apps/tours/web/tests/setup.ts — afterEach(cleanup) for vitest
    - .github/workflows/release.yml — matrix refactor D-04
    - .env.example — NEXTAUTH_SECRET explicit comment + DATABASE_URL context

key-decisions:
  - "Default global row Eliminar disabled via `disabled` attribute + `title='Regla global por defecto — no eliminable'` — single selector `getAllByTitle` in tests avoids ambiguity with row text"
  - "Schema isolation test pass requires `async_session` fixture (which calls _create_schema) not plain `async_engine` — direct engine connect sees an empty in-memory DB without schema"
  - "Catalogos special-case steering for `comisiones` happens inside the page route (return early with ComisionesTab) — simpler than splitting to a different route; the ComisionesTab component owns its own sub-nav rendering"
  - "LiquidacionAsientos is NOT auditable (`__auditable__ = False`) — audit_log captures Asientos writes individually; pivot is bookkeeping only"
  - "DELETE /tours_servicios hard-deletes the row but leaves the asiento_id FK intact (ondelete=RESTRICT) — the audit_log + asiento record remain for traceability. Plan 02 simplification — future plan may emit manual reversal asiento on DELETE"
  - "Workflow `paths:` include `apps/landing/** + apps/tours/** + .github/workflows/release.yml` — both apps trigger; matrix runs all 3 apps even if only one changed (over-engineering paths-filter to suppress matrix apps deferred per Plan 02 note — VPS runs once per main push)"
  - "Dashboard RBAC role-forcing covers only vendedor; contabilidad role behaves as admin for dashboard read-only (SC#2 restricts audit_log to admin, NOT dashboard — T-02.1-14 explicitly only forces `vendedor`)"
  - "Estado enum uses Spanish per UI-SPEC (abierta|cerrada|revertida); inconsistency with English `reverted` in Plan prose resolved to enum-driven Spanish to match StatusBadge variants"

patterns-established:
  - "Pattern: LiquacionAsientos pivot + asiento_id metadata-via-asiento_original_id enables clean reopen reverse without rewriting Asientos rows — same model reuseable for future reversible operations"
  - "Pattern: per-vendedor margin commission loop in close_liquidacion — one post_asiento call per vendedor keeps each asiento atomic and 501/201 atomic per (closing) vendedor"
  - "Pattern: matrix CI build with `include` providing per-app context/file/image_suffix — different Dockerfile contexts (root vs subfolder) coexist in one matrix"
  - "Pattern: GET endpoints with `?vendedor_id=N` ALWAYS force for non-admin — verifier must hit both `/dashboard/saldos` AND `/dashboard/tours_pendientes` with the regression test"

requirements-completed: [TOURS-03, TOURS-07, TOURS-09, TOURS-10]

coverage:
  - id: D1
    description: "Liquidaciones state machine: POST /liquidaciones auto-assigns tours in range; POST /close generates asientos de comisión 501/201; POST /reopen swaps debe/haber + estado=revertida + unlock tours"
    requirement: TOURS-09
    verification:
      - kind: unit
        ref: "apps/tours/api/tests/test_liquidaciones.py::test_close_genera_asientos"
        status: pass
      - kind: unit
        ref: "apps/tours/api/tests/test_liquidaciones.py::test_close_precheck_aborts"
        status: pass
      - kind: unit
        ref: "apps/tours/api/tests/test_liquidaciones.py::test_reopen_reverts"
        status: pass
      - kind: unit
        ref: "apps/tours/api/tests/test_liquidaciones.py::test_reopen_then_reclose_generates_codigo_incrementado"
        status: pass
      - kind: unit
        ref: "apps/tours/api/tests/test_liquidaciones.py::test_tour_en_cerrada_no_editable"
        status: pass
    human_judgment: false
  - id: D2
    description: "Comisiones precedence 4 levels + default global non-deletable + /simular preview"
    requirement: TOURS-07
    verification:
      - kind: unit
        ref: "apps/tours/api/tests/test_comisiones.py::test_precedence_all_4_levels"
        status: pass
      - kind: unit
        ref: "apps/tours/api/tests/test_comisiones.py::test_default_global_no_deletable"
        status: pass
      - kind: unit
        ref: "apps/tours/api/tests/test_comisiones.py::test_simular_margen"
        status: pass
    human_judgment: false
  - id: D3
    description: "Schema isolation SC#9: tablas core no FK a tours; tours FK a core (asiento_id, usuario_id via audit_log) + LiquidacionAsientos (tours-only)"
    requirement: TOURS-10
    verification:
      - kind: unit
        ref: "apps/tours/api/tests/test_schema_isolation.py::test_schema_isolation_core_vs_tours"
        status: pass
    human_judgment: false
  - id: D4
    description: "Dashboard S2: 4 content-cards + filter bar + saldos + tours pendientes + RBAC role-forcing (T-02.1-14)"
    requirement: TOURS-09
    verification:
      - kind: unit
        ref: "apps/tours/api/tests/test_dashboard.py::test_saldos_filter_by_fecha"
        status: pass
      - kind: unit
        ref: "apps/tours/api/tests/test_dashboard.py::test_saldos_filter_by_vendedor"
        status: pass
      - kind: unit
        ref: "apps/tours/api/tests/test_dashboard.py::test_tours_pendientes"
        status: pass
      - kind: unit
        ref: "apps/tours/api/tests/test_dashboard.py::test_dashboard_vendedor_forced_to_self"
        status: pass
      - kind: automated_ui
        ref: "apps/tours/web/tests/dashboard.test.tsx — 4 card titles + filter bar present"
        status: pass
    human_judgment: true
    rationale: "Backend role-forcing unit-tested + UI render assertions; visual adherence to UI-SPEC (color intensity 30/60/90, badge translations) requires end-user phase-level UAT"
  - id: D5
    description: "Auditoría viewer S6 admin-only with JSON expandable + password_hash null"
    verification:
      - kind: automated
        ref: "cd apps/tours/web && pnpm build — App Router /admin/auditoria route declared, build clean"
        status: pass
    human_judgment: true
    rationale: "Browser-only behavior (toggle 'Ver datos' expand, JSON pretty-print render, password_hash null display) is not asserted by automated UI test; this is part of user end-of-phase UAT"
  - id: D6
    description: "Catálogos S5 Comisiones tab + sub-nav 6 tabs + default global row Eliminar disabled with tooltip"
    verification:
      - kind: automated_ui
        ref: "apps/tours/web/tests/catalogos.test.tsx — 6 tabs + default global tooltip + Origen 4-class"
        status: pass
    human_judgment: false
  - id: D7
    description: "CI matrix refactor (D-04): release.yml matrix over [landing, tours-web, tours-api] with tours-api context=apps/tours/api + deploy SSH matrix-aware pull"
    verification:
      - kind: automated
        ref: "python3 -c yaml.safe_load(release.yml) — YAML valid, matrix apps count=3, tours-api context=apps/tours/api (BLOCKER fix verified)"
        status: pass
    human_judgment: false
  - id: D8
    description: "apps/tours/README.md with Dev/Build/Test/Schema migrations/Deploy/Endpoints/Decisions sections"
    verification:
      - kind: automated
        ref: "test -f apps/tours/README.md && grep -c '## Deploy (CI)' && grep -c '## Schema migrations' — both > 0"
        status: pass
    human_judgment: false
  - id: D9
    description: ".env.example reviewed: NEXTAUTH_SECRET generation comment explicit + DATABASE_URL path"
    verification:
      - kind: automated
        ref: "grep 'NEXTAUTH_SECRET' .env.example shows 'openssl rand -base64 32' + 'MISMO valor en tours-web y tours-api (D-02)' note"
        status: pass
    human_judgment: false
  - id: D10
    description: "Post-deploy HTTPS verification: `curl -I https://tours.luciel.dev` returns 200 + Let's Encrypt R3/E1 issuer (SC#1)"
    verification: []
    human_judgment: true
    rationale: "Requires user trigger of the GitHub Actions release.yml matrix build → SSH deploy job → real VPS HTTPS cert provisioning. Agent cannot run prod deploy or browser-based login flow — this is the Task 4 checkpoint:human-verify content, deferred to end-of-phase UAT."

duration: 23 min
completed: 2026-07-05
status: complete
---

# Phase 02.1 Plan 02: Liquidaciones + Dashboard + Auditoría + CI Matrix Summary

**Liquidaciones state machine (close/reopen with auto-generated asientos de comisión 501/201 per vendedor + asientos de reversión swap debe/haber), dashboard S2 with RBAC backend role-forcing, auditoría viewer S6 with JSON expander, Catálogos Comisiones tab S5, CI matrix refactor (D-04) over [landing, tours-web, tours-api] GHCR images, apps/tours/README.md**

## Performance

- **Duration:** 23 min (2026-07-05T00:09:06Z → 2026-07-05T00:32:29Z)
- **Started:** 2026-07-05T00:09:06Z
- **Completed:** 2026-07-05T00:32:29Z
- **Tasks:** 3 of 4 (Task 4 deferred to user end-of-phase UAT — checkpoint:human-verify blocking)
- **Files modified:** 26 files (8 new files in backend tests, 11 new in web, 4 modified, 1 README, 1 env, 1 workflow)

## Accomplishments

- Backend liquidaciones state machine: close (auto-genera asientos equilibrados per-vendedor debitando 501-COSTOS-COMISIONES y acreditando 201-COMISIONES-POR-PAGAR, asigna codigo LIQ-AAAA-NNN incremental por año, persiste LiquidacionAsientos pivot tipo='cierre') + reopen (swap debe/haber del cierre original, metadata.reversion=true + asiento_original_id, estado=revertida, reopen_count += 1, tours_servicios.liquidacion_id NULL unlock)
- Close pre-checks abortan con 422 + lista de problemas específica si costos > 0 faltantes; USD tours sin TC interno previo generan warning (warn-only per Simplificación 2.1) — admin decide
- POST /liquidaciones auto-asigna tours_servicios del rango con liquidacion_id IS NULL + filtros opcionales (D-19)
- PUT/DELETE /tours_servicios/{id} con liquidacion.estado='cerrada' → 409 `Tour en liquidación cerrada, reabre primero` (D-14)
- Dashboard endpoints /dashboard/saldos (GROUP BY cuentas with SUM(debe)-SUM(haber) + filtros fecha/agencia/vendedor/moneda) + /dashboard/tours_pendientes (tours with liquidacion_id IS NULL, ordered by fecha asc, `dias_desde_venta` field per D-20)
- RBAC backend role-forcing (T-02.1-14): `if user['role'] == 'vendedor': vendedor_id = int(user['id'])` — vendedor cannot read another vendedor's dashboard data via direct curl `?vendedor_id=99`. Admin/contabilidad exempt. Test test_dashboard_vendedor_forced_to_self covers both endpoints + admin JWT baseline.
- LiquidacionAsientos pivot table added (no audit_log noise — `__auditable__ = False`); reversible tracing via metadata.asiento_original_id
- Dashboard S2 (Next.js): 4 content-cards (Caja Soles/Dólares/Ingresos Tours/Costos Tours) Playfair 28 tabular-nums negativos chili-red + filter bar (fecha_desde/hasta/agencia/vendedor/moneda + Aplicar/Limpiar filtros) + saldos table sticky-header Wine + tours pendientes section with Días desde venta chili-red intensifying past 30/60/90 days (animate-pulse at ≥90)
- Liquidaciones S4: list with status badges (abierta amber-warning / cerrada wine-muted / revertida stone-wall) + detail page with LIQ-AAAA-NNN Playfair 28 header + pre-check strip (amber-warning warnings / chili-red blockers) + tours table inside + footer CloseModal (Playfair 24 `Cerrar liquidación LIQ-AAAA-NNN`, asientos preview, warn copy irreversibilidad + Reabrir path, Confirmar cierre disabled on blockers) + ReopenModal (D-13 copy + codigo incremental note)
- Auditoría viewer S6 (admin only): filter bar (Usuario/Tabla/Acción selects from catalog list) + dense table + Ácción StatusBadge variants (I/U/D mapped to wine-light-tint/gold-lightest/chili-tint) + AuditRowExpander (`Ver datos` text-link toggles JSON pretty-printed Nunito 13 monospace in stone-wall panel) — applies `password_hash = null` redaction per D-26 explicitly surfaced
- Catálogos/[entidad] extended: 6-tab sub-nav pills (Agencias · Tours · Vendedores · Formas de pago · Monedas · Comisiones, Wine fill active) with `comisiones` rendered via ComisionesTab component (separate from generic DataTable catalog path). Origen column classifies each rule 4-class; default global row has Eliminar disabled with `title='Regla global por defecto — no eliminable'` per D-10
- ComisionReglaModal component (optional vendedor+tour selects, porcentaje number, precedencia slot preview "Esta regla se aplicará cuando vendedor=X y tour=Y" or "global")
- CI matrix refactor (D-04): release.yml strategy.matrix over [landing, tours-web, tours-api] with `include` providing context/file/image_suffix per matrix entry. tours-api context=apps/tours/api (NOT '.') — BLOCKER fix per Plan 02 acceptance criteria (Plan 01 Dockerfile is self-contained with `COPY pyproject.toml / COPY app/`). Deploy job is matrix-aware single SSH that pulls all 3 services.
- apps/tours/README.md full doc: Stack (Next.js 16 + FastAPI + SQLite WAL + Traefik), Dev local, Build (root vs subfolder context callout), Test (pytest + vitest + pnpm build), Schema migrations (Alembic + lifespan create_all fallback), Deploy (CI), Endpoints API table (all 14+ endpoints with roles), Decisions list linking to 02.1-CONTEXT.md D-XX.
- .env.example reviewed and expanded: NEXTAUTH_SECRET generation comment now explicit ("openssl rand -base64 32 — ≥32 bytes, MISMO valor en tours-web y tours-api (D-02)"), DATABASE_URL context clarification (named Docker volume tours-db-data mounted at /data), ADMIN_INITIAL_PASSWORD seed-only note
- 32 pytest tests green (13 new in this plan + 19 from Plan 01 preserved); 6 vitest tests green (2 dashboard + 3 catalogos + 1 smoke); pnpm build clean — no TS errors

## Task Commits

Each task was committed atomically with TDD gate discipline for Tasks 1 + 2 (`tdd="true"`):

1. **Task 1 RED** — `ec88465` (test) — add failing tests for liquidaciones + dashboard + comisiones + schema isolation + LiquidacionAsientos model pivot + endpoints stubbed 501
2. **Task 1 GREEN** — `e6a9035` (feat) — implement close/reopen + dashboard + RBAC backend role-forcing
3. **Task 2 RED** — `9a03b74` (test) — add failing tests for DashboardCards + ComisionesTab components
4. **Task 2 GREEN** — `fabe6e1` (feat) — implement dashboard S2 + liquidaciones S4 + auditoría S6 + Catálogos S5 extension
5. **Task 3** — `bc32dfe` (feat) — CI matrix refactor + .env.example review + apps/tours/README.md

**Plan metadata commit:** pending STATE.md / ROADMAP.md updates after this SUMMARY (next commit).

## Files Created/Modified

### Backend (apps/tours/api)
- `app/services/liquidaciones.py` (new) — close_liquidacion + reopen_liquidacion + get_precheck + LiquidacionPrecheckError
- `app/models/tours.py` — added `LiquidacionAsientos` pivot (non-auditable) with tipo='cierre'|'reversion'
- `app/routers/tours.py` — POST /liquidaciones auto-assign + close/reopen/precheck + dashboard/saldos + dashboard/tours_pendientes + PUT/DELETE /tours_servicios/{id} + list_liquidaciones filters
- `tests/test_liquidaciones.py` (new) — 5 tests covering close/reopen/state machine/D-14 lock
- `tests/test_comisiones.py` (new) — 3 tests covering precedence 4 levels + default non-deletable + /simular
- `tests/test_schema_isolation.py` (new) — 1 test (SC#9)
- `tests/test_dashboard.py` (new) — 4 tests incl T-02.1-14 regression

### Frontend (apps/tours/web)
- `src/app/(app)/page.tsx` — replaced placeholder with S2 dashboard full page
- `src/app/(app)/_components/DashboardCards.tsx` (new) — 4-card + filter bar + reusable component
- `src/app/(app)/liquidaciones/page.tsx` (new) — S4 list with status badges
- `src/app/(app)/liquidaciones/[id]/page.tsx` (new) — S4 detail with pre-check strip + footer actions
- `src/app/(app)/liquidaciones/[id]/components/CloseModal.tsx` (new) — Playfair 24 + asientos preview + warn copy
- `src/app/(app)/liquidaciones/[id]/components/ReopenModal.tsx` (new) — D-13 copy + codigo incremental note
- `src/app/(app)/admin/auditoria/page.tsx` (new) — admin-only S6 with filter + dense table + JSON expander
- `src/app/(app)/admin/auditoria/_components/AuditRowExpander.tsx` (new) — Ver datos toggle
- `src/app/(app)/catalogos/_components/ComisionesTab.tsx` (new) — S5 sub-component with 6-tab nav + Origen column
- `src/app/(app)/catalogos/[entidad]/page.tsx` — extended to special-case `comisiones` rendering
- `src/components/comisiones/ComisionReglaModal.tsx` (new) — precedencia slot preview modal
- `tests/dashboard.test.tsx` (new) — 2 tests (4 cards + filter bar)
- `tests/catalogos.test.tsx` (new) — 3 tests (6 tabs + global non-deletable + Origen classification)
- `tests/setup.ts` — afterEach(cleanup) to prevent vitest render accumulation

### Infra
- `.github/workflows/release.yml` — D-04 matrix refactor over [landing, tours-web, tours-api]; tours-api context=apps/tours/api BLOCKER fix
- `.env.example` — NEXTAUTH_SECRET explicit comment + DATABASE_URL context note
- `apps/tours/README.md` (new) — full Dev/Build/Test/Schema migrations/Deploy/Endpoints/Decisions

## Decisions Made

- Estado enum: Spanish `abierta|cerrada|revertida` per UI-SPEC S4 StatusBadge variants; plan prose English "reverted" is treated as the same state (tester should expect `revertida` everywhere model+API exposes the value)
- Catalogos page routes `comisiones` to ComisionesTab component by early-return instead of separate route — keeps existing `/catalogos/[entidad]/[id]/...` pattern that future entities could also override
- Dashboard RBAC role-forcing covers only `vendedor` role — `contabilidad` is exempt (treated as admin for read-only dashboard because SC#2 restricts audit_log to admin only, NOT dashboard). Test verifies admin + vendedor only.
- LiquidacionAsientos is NOT Auditable — pivot is bookkeeping for clean reverse lookups; the underlying Asientos are auditable so audit_log captures writes there
- After their tests `screen.getByText("vendedor+tour")` was failing "Found multiple elements" between tests — root cause was missing @testing-library/react cleanup; fixed via `afterEach(cleanup)` in tests/setup.ts (documented as pattern established)
- CI deploy job is matrix-aware SINGLE SSH pulling all 3 services (NOT per-app SSH matrix deploys — those would race on the same VPS Docker daemon)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] test_schema_isolation needed `async_session` not `async_engine` to access schema**
- **Found during:** Task 1 GREEN phase
- **Issue:** Test fixture `async_engine` alone does not call `_create_schema` — the in-memory DB has no tables when inspected via `insp.get_table_names()`. The `async_session` fixture calls `_create_schema` during setup, so tables exist.
- **Fix:** Changed test fixture parameter from `async_engine` to `async_session`; accessed engine via `async_session.bind` for the raw connection + inspect call.
- **Files modified:** `apps/tours/api/tests/test_schema_isolation.py`
- **Verification:** `pytest tests/test_schema_isolation.py -x` passes — all 9 core+tours tables detected
- **Committed in:** e6a9035

**2. [Rule 3 - Blocking] vitest accumulating renders between tests — `getAllByText` "Found multiple elements"**
- **Found during:** Task 2 GREEN phase
- **Issue:** Without `afterEach(cleanup)` in tests/setup.ts, React renders from previous `it` blocks persisted in the same DOM container; the third catalogos test reported "Found 3 matching elements" when only one row had the expected text.
- **Fix:** Added `afterEach(() => { cleanup() })` to `apps/tours/web/tests/setup.ts` per @testing-library/react convention.
- **Files modified:** `apps/tours/web/tests/setup.ts`
- **Verification:** `pnpm vitest run` — all 6 tests green
- **Committed in:** fabe6e1

**3. [Rule 1 - Bug] dashboard_saldos handler referenced `func.sum` without importing `func` in routers/tours.py**
- **Found during:** Task 1 GREEN phase first test run
- **Issue:** Dashboard saldos query uses `func.sum(AsientoLineas.debe)` but `from sqlalchemy import select` did not include `func`; NameError at runtime.
- **Fix:** Added `func` to the sqlalchemy import in `apps/tours/api/app/routers/tours.py`.
- **Files modified:** `apps/tours/api/app/routers/tours.py`
- **Verification:** `pytest tests/test_dashboard.py -x` passes
- **Committed in:** e6a9035

**4. [Rule 1 - Bug] test `assert body["estado"] == "reverted"` was wrong enum — model uses Spanish `revertida`**
- **Found during:** Task 1 GREEN phase
- **Issue:** Plan prose uses English "reverted" but EstadoLiquidacion enum + UI-SPEC S4 StatusBadge use Spanish `revertida`. Tests expected English.
- **Fix:** Changed both Python test assertions (`test_reopen_reverts` body.estado + persisted liq_row.estado.value) to expect "revertida".
- **Files modified:** `apps/tours/api/tests/test_liquidaciones.py`
- **Verification:** `pytest tests/test_liquidaciones.py::test_reopen_reverts -x` passes
- **Committed in:** e6a9035

**5. [Rule 1 - Bug] dashboard.test.tsx SaldosRow type missing `id` — DataTable requires `{ id: number }`**
- **Found during:** Task 2 GREEN phase `pnpm build`
- **Issue:** DataTable generic requires `T extends { id: number }` but SaldosRow didn't include `id`. TS compile failed on root `page.tsx` rendering `<DataTable columns={saldosColumns} data={saldos} />`.
- **Fix:** Added `id: number` + optional `total_debe` + `total_haber` to SaldosRow type; backend returns all three.
- **Files modified:** `apps/tours/web/src/app/(app)/_components/DashboardCards.tsx`
- **Verification:** `pnpm build` exits 0
- **Committed in:** fabe6e1

---

**Total deviations:** 5 auto-fixed (3 Rule 3 blocking, 2 Rule 1 bugs)
**Impact on plan:** All auto-fixes necessary for build/test passage. No scope creep. Two deviations (test fixture `async_session`, vitest cleanup) corrected test-harness gaps not visible in plan prose. One deviation (Spanish enum) resolved plan prose inconsistency.

## Issues Encountered

- None beyond the 5 deviations above. Plan executed cleanly once tests + setup issues were fixed.
- Note: Pre-existing untracked files in repo root NOT touched by this plan — `DESIGN.md`, `luciel-platform-brief.md`, `ssh-key-2026-07-03.key`, `ssh-key-2026-07-03.key.pub`, `.planning/phases/02.1-tours-panel-contable-hotel/.gitkeep`, `.planning/phases/02.1-tours-panel-contable-hotel/02.1-PATTERNS.md`. The 02.1-02 files were created by `/gsd-plan-phase`; the SSH keys appear to be locally-created scratch files that should be added to `.gitignore` per repo policy (`ssh-key-*.key`, `ssh-key-*.key.pub`). Flagged for the user but not acted upon (scope boundary — pre-existing files outside this plan's file list).

## User Setup Required

**Action required for Task 4 (`checkpoint:human-verify` blocking):**

The user needs to:
1. Merge Plan 02.1-02 commits to main (which triggers the release.yml matrix build)
2. Wait for the `build-and-push` matrix + `deploy` SSH jobs in GitHub Actions Actions tab to complete successfully
3. Run `curl -I https://tours.luciel.dev` — expected 200 with `server: Traefik` + `strict-transport-security` headers
4. Verify `openssl s_client -connect tours.luciel.dev:443 -servername tours.luciel.dev < /dev/null 2>/dev/null | openssl x509 -noout -subject -issuer` shows `issuer=CN=R3` or `issuer=CN=E1` (Let's Encrypt production chain — NOT self-signed)
5. Open `https://tours.luciel.dev/login` in browser, login with `admin@tours.luciel.dev` + `ADMIN_INITIAL_PASSWORD` (from `.env`), and run through the 8 verification steps in PLAN.md Task 4 `<how-to-verify>` (dashboard renders, /ventas flow, liquidación close/reopen, auditoría viewer, comisiones tab default global non-deletable, RBAC vendedor redirect)

External service `VPS deploy (HTTPS Let's Encrypt real)` — same secret set (`VPS_HOST` / `VPS_USER` / `VPS_SSH_KEY` / `DEPLOY_PATH`) as configured for Phase 01-02; no new secrets.

DNS wildcard `*.luciel.dev` already configured in Phase 1 — `tours.luciel.dev` resolves automatically (no DNS changes needed).

## Next Phase Readiness

- Phase 02.1 plan 2 of 2 complete — phase 02.1 ready for phase verification (Task 4 checkpoint content deferred to user end-of-phase UAT)
- The code side is production-ready; tasks 1-3 cover the entire implementation contract (backend + UI + CI + docs)
- After user verifies prod HTTPS + login + end-to-end flows, phase 02.1 can be marked complete

**Manual verification (Task 4) deferred to end-of-phase UAT per workflow.human_verify_mode=end-of-phase (#3309):**
- curl -I https://tours.luciel.dev 200 + LE cert issuer (SC#1)
- Login browser + flujo ventas → toast asiento_id (SC#3 audit)
- Liquidación close/reopen e2e (SC#5)
- Comisiones reglas manager default non-deletable (SC#6)
- Dashboard filterable (SC#7)
- Auditoría viewer admin-only + JSON expandable + password_hash null (SC#2 / SC#4)
- RBAC vendedor redirect + nav hide (SC#2)
- Catálogos 6 tabs no texto libre in forms (SC#8)
- Reproducible via `docker compose up -d` clean checkout (SC#10)

---
*Phase: 02.1-tours-panel-contable-hotel*
*Completed: 2026-07-05*

## Self-Check: PASSED

- All 17 key-files created/modified were verified on disk with `[ -e ]` — FOUND for every file in the frontmatter `key-files.created` / `key-files.modified` lists.
- All 5 task commits (`ec88465`, `e6a9035`, `9a03b74`, `fabe6e1`, `bc32dfe`) were verified via `git log --oneline --grep="02.1-02"` — each commit hash reachable from HEAD.
- Backend pytest: 32 tests passed (13 new in this plan + 19 from Plan 01 preserved).
- Frontend vitest: 6 tests passed (2 dashboard + 3 catalogos + 1 smoke).
- `pnpm build` (apps/tours/web) exits 0 — no TypeScript errors, all routes compile (/, /admin/auditoria, /admin/usuarios, /liquidaciones, /liquidaciones/[id], /catalogos/[entidad], /ventas, /login, /api/*).
- `.github/workflows/release.yml` YAML validated via `python3 -c yaml.safe_load` — schema OK, matrix apps count=3, tours-api matrix entry has `context: apps/tours/api` (BLOCKER fix verified).
- TDD gate compliance: RED commits (`test(02.1-02)`) precede GREEN commits (`feat(02.1-02)`) for both `tdd="true"` tasks (Task 1 + Task 2) per git log time order.