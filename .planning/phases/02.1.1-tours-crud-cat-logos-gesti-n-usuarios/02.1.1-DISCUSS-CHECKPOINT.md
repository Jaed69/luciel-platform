# Phase 02.1.1 — Discuss checkpoint

**Saved:** 2026-07-05
**Status:** Gray areas presented, awaiting user selection
**Next step:** User selects which gray areas to discuss

## Phase scope (confirmed)

Phase 02.1.1 = completar CRUD funcional para:
1. Catálogos (agencias, tours, vendedores, formas-pago, monedas) — backend GET/POST/DELETE existe, falta PUT
2. Usuarios — backend SIN endpoints, frontend mock estático
3. Frontend wiring real (modals, forms, delete confirm, toasts)

## Codebase state (scouted)

### Backend — what exists
- `apps/tours/api/app/routers/core.py`:
  - `GET /catalogos/{entidad}` — lista (cualquier autenticado)
  - `POST /catalogos/{entidad}` — crea (admin)
  - `DELETE /catalogos/{entidad}/{row_id}` — soft delete activo=false (admin)
  - **NO PUT** — editar no existe
  - Short aliases: `/agencias`, `/vendedores`, `/tours`, `/formas-pago`, `/monedas` (GET only)
- `apps/tours/api/app/models/core.py`:
  - `Usuarios(Base, Auditable)` — `__tablename__="usuarios"`, `__sensitive_fields__={"password_hash"}` (D-26)
  - Fields: `email: String(128) unique`, `password_hash: String(128)`, plus inherited Auditable fields
- `/usuarios` CRUD endpoints — **NO existen** en routers. Solo `/auth/login` (auth.py).

### Frontend — what exists (placeholder)
- `apps/tours/web/src/app/(app)/catalogos/[entidad]/page.tsx`:
  - Server component, fetches `/api/catalogos/[entidad]` (proxy route)
  - Renders DataTable with columns: Código, Nombre, Estado, Acciones
  - **Botones "Agregar", "Editar", "Restaurar" son `<a href="#">` sin handler**
  - Sub-nav tabs: agencias, tours, vendedores, formas-pago, monedas, comisiones
- `apps/tours/web/src/app/(app)/admin/usuarios/page.tsx`:
  - **Mock estático** — array fijo con solo admin@tours.luciel.dev
  - Botones "Nuevo usuario", "Editar", "Restablecer contraseña", "Eliminar" todos `<a href="#">`
  - Comentario en código confirma: "MVP slice shows a static table for now. The backend /usuarios endpoint isn't implemented"
- `apps/tours/web/src/app/api/catalogos/[entidad]/route.ts`:
  - Proxy GET → FastAPI con Bearer token (fix c725cf4 ya pusheado)
- `apps/tours/web/src/app/api/simular/route.ts` + `/ventas/route.ts` — mismo patrón proxy

### Patrones reusables
- `ComisionReglaModal` (`apps/tours/web/src/components/comisiones/ComisionReglaModal.tsx`) — **único catálogo funcional**, modal pattern referencia
- `VentaFormModal` — modal create con comisión preview live, patrón form modal
- `Button`, `DataTable`, `StatusBadge`, `Modal`, `Toast`, `FloatingLabelInput`, `ContentCard`, `Skeleton` — componentes UI ya implementados
- Soft delete pattern: `activo=false` en DELETE, UI muestra "Activo"/"Inactivo" con link "Restaurar"

## Prior decisions carrying forward (from 02.1-CONTEXT.md)

- **D-01:** 2 containers (tours-web + tours-api), FastAPI interno
- **D-02:** Auth bridge NextAuth JWT + pyjwt verification (fix c725cf4 firmado HS256)
- **D-21/D-23:** Audit log via SQLAlchemy before_flush + ContextVar `current_user_id`
- **D-22:** `usuarios` tiene mixin Auditable → todo CRUD queda auditado automático
- **D-24:** Solo role `admin` ve audit_log
- **D-26:** `password_hash` redacted → null en audit_log snapshots
- **Soft delete** (activo=false) ya implementado en catálogos DELETE
- Componentes UI: paleta wine/gold/stone + font-playfair/font-nunito (UI-SPEC)

## Gray areas presented (awaiting user selection)

### 1. Usuarios CRUD — scope y campos
- ¿Campos del usuario? (email, username, password, role, activo, último_acceso)
- ¿Qué roles pueden crear/editar usuarios? (solo admin, o admin+contabilidad)
- ¿Reset de password? (admin resetea, o usuario cambia su propio password)
- ¿Endpoint para cambiar propio password?

### 2. Catálogos edit (PUT endpoint)
- Backend tiene GET/POST/DELETE pero **no PUT**. ¿Agregar?
- ¿Campos editables? (codigo, nombre, activo)
- ¿Soft delete ya OK (activo=false) — UI muestra togglear "ver inactivos"?
- ¿Restaurar item inactivo = PUT activo=true o endpoint dedicado?

### 3. UI patterns — modal vs inline
- ¿Crear/editar en modal (como ComisionReglaModal) o página dedicada?
- ¿Confirmación antes de eliminar? (toast "¿Seguro?" o modal confirmación)
- ¿Feedback post CRUD? (toast success + refetch lista)
- ¿DataTable ya existente reutilizado o nuevo?

### 4. Validaciones y constraints
- ¿Campos únicos? (email único en usuarios, codigo único en catálogos)
- ¿Password strength? (mínimo 8 chars, o más complejo)
- ¿Eliminar catálogo en uso? (ej: agencia con ventas → bloquear o permitir soft delete)
- ¿Eliminar usuario con ventas registradas? (bloquear o soft delete)

## Resume instructions

Next session: read this file, ask user which gray areas (1-4) to discuss, deep-dive each, write CONTEXT.md.

User said: "guardalos, para continuar en la nueva sesion clear"