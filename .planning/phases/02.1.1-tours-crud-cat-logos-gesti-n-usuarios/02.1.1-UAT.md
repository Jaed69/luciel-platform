---
status: testing
phase: 02.1.1-tours-crud-cat-logos-gesti-n-usuarios
source: [02.1.1-01-SUMMARY.md, 02.1.1-02-SUMMARY.md]
started: 2026-07-05T00:00:00Z
updated: 2026-07-05T00:00:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 2
name: Backend CRUD Surface — Confirmation (8 deliverables auto-verified)
expected: |
  All 8 backend deliverables from Plan 02.1.1-01 are covered by passing
  unit tests (26 new tests, 58 total green). Confirm the surface is
  functionally correct: PUT /catalogos preserves activo + RBAC; DELETE
  /catalogos referential 409 with detail.referencias; /usuarios CRUD +
  2 password endpoints; bcrypt hashing; audit_log password redaction
  (D-26); last-admin + self-delete + email-unique guards.
awaiting: user response

## Tests

### 1. Cold Start Smoke Test (Backend)
expected: Start FastAPI + Next dev from cold state. uvicorn boots without errors, bcrypt/audit listener load, primary smoke query (health check or GET /catalogos/monedas as admin) returns live data. Next dev compiles, /perfil and /admin/usuarios routes 200.
result: skipped
reason: user will test in production

### 2. Backend CRUD Surface — Confirmation (8 deliverables auto-verified)
expected: All 8 backend deliverables from Plan 02.1.1-01 are covered by passing unit tests (26 new tests, 58 total green). Confirm the surface is functionally correct: PUT /catalogos preserves activo + RBAC; DELETE /catalogos referential 409 with detail.referencias; /usuarios CRUD + 2 password endpoints; bcrypt hashing; audit_log password redaction (D-26); last-admin + self-delete + email-unique guards.
result: [pending]

### 3. Frontend RBAC Live Flow (proxyJson + 5 Route Handlers)
expected: End-to-end RBAC flow against a live server (dev or prod HTTPS). Logging in as admin/contabilidad/vendedor, hitting /api/catalogos and /api/usuarios route handlers, the JWT bearer is forwarded correctly to FastAPI, 4xx responses surface the toast (especially the 409 with detail.referencias on DELETE in-use), cookies/sessions persist across reload. Logout clears the session.
result: [pending]

### 4. CatalogoFormModal — Agregar + Editar
expected: On /catalogos/[entidad] (non-comisiones), clicking "Agregar" opens CatalogoFormModal with empty fields. Submitting POST /api/catalogos creates the row and the table refreshes (toast confirms). Clicking "Editar" on a row opens the modal pre-filled with codigo/nombre; PUT /api/catalogos/{entidad}/{id} updates the row, activo preserved. Attempting to DELETE a row that is referenced by an FK surfaces the 422/409 with detail.referencias in the toast.
result: [pending]

### 5. /catalogos/[entidad] Restaurar action
expected: For an inactive catalog row (e.g. a soft-deleted moneda or cuenta), the "Restaurar" action reactivates it (POST /api/catalogos/{entidad}/{id}/restore → activo=true). For monedas (no activo column) the backend 422s and the toast/esperada message shows the monedas-specific error. Rows without a Restaurar action stay hidden (no UI affordance for non-restorable entities).
result: [pending]

### 6. PUT /catalogos/{entidad}/{id} (auto)
expected: Updates codigo/nombre, preserves activo (D-03); admin+contabilidad RBAC (D-13); vendedor 403.
result: pass
source: automated
coverage_id: D1

### 7. DELETE /catalogos/{entidad}/{id} referential check (auto)
expected: 409 + detail.{mensaje, referencias} when FKs reference; soft-delete otherwise; monedas blocked if has cuentas; RBAC admin+contabilidad, vendedor 403.
result: pass
source: automated
coverage_id: D2

### 8. POST /usuarios (auto)
expected: bcrypt-hashed password; 409 on duplicate email; audit 'I' row with password_hash null; vendedor 403.
result: pass
source: automated
coverage_id: D3

### 9. GET /usuarios (auto)
expected: Admin-only (D-15); list excludes password_hash (D-26).
result: pass
source: automated
coverage_id: D4

### 10. PUT /usuarios/{id} (auto)
expected: Admin-only; updates email/username/rol/acto; extra='forbid' rejects password field; last-admin role-change 409 (D-11); vendedor 403.
result: pass
source: automated
coverage_id: D5

### 11. DELETE /usuarios/{id} (auto)
expected: Soft-delete (activo=false); self-delete 409 (D-12); last-admin 409 (D-11); audit 'U' row; vendedor 403.
result: pass
source: automated
coverage_id: D6

### 12. PUT /usuarios/me/password (auto)
expected: Any authed user; verifies current_password; audit 'U' with redacted hash (D-26); wrong current 401.
result: pass
source: automated
coverage_id: D7

### 13. PUT /usuarios/{id}/password (auto)
expected: Admin-only reset; no current required; non-admin 403.
result: pass
source: automated
coverage_id: D8

### 14. /perfil page + PasswordForm (auto)
expected: 3 inputs; inline validation (<8 chars, mismatch); correct PUT /api/usuarios/me/password body; toast "Contraseña actual incorrecta" on 401.
result: pass
source: automated
coverage_id: D3

### 15. /admin/usuarios rewrite (auto)
expected: Fetches /api/usuarios; renders rows with email; Editar opens modal; Eliminar disabled for self/last-admin/inactive.
result: pass
source: automated
coverage_id: D4

### 16. Nav Perfil + Catálogos RBAC (auto)
expected: Perfil visible to all 3 roles; Catálogos visible to admin+contabilidad only.
result: pass
source: automated
coverage_id: D6

## Summary

total: 16
passed: 11
issues: 0
pending: 4
skipped: 1

## Gaps

[none yet]