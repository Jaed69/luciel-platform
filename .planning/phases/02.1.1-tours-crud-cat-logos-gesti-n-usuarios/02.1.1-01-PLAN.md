---
phase: 02.1.1-tours-crud-cat-logos-gesti-n-usuarios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/tours/api/app/routers/core.py
  - apps/tours/api/app/routers/usuarios.py
  - apps/tours/api/app/schemas/core.py
  - apps/tours/api/app/main.py
  - apps/tours/api/tests/test_catalogos.py
  - apps/tours/api/tests/test_usuarios.py
autonomous: false
requirements: [TOURS-04, TOURS-06, TOURS-08]
must_haves:
  truths:
    - "PUT /catalogos/{entidad}/{id} updates codigo/nombre and preserves activo; admin and contabilidad can call it; vendedor gets 403"
    - "DELETE /catalogos/{entidad}/{id} returns 409 with {detail.mensaje, detail.referencias} when the item is referenced; returns 200 with {ok: true} and soft-deletes (activo=false) when not in use"
    - "POST /usuarios creates a user with bcrypt-hashed password; duplicate email returns 409; vendedor gets 403"
    - "PUT /usuarios/{id} updates email/username/rol/activo (no password); admin only; rejects role change that would leave zero admins"
    - "DELETE /usuarios/{id} soft-deletes (activo=false); admin only; rejects last-admin and self-delete"
    - "PUT /usuarios/me/password requires correct current_password for any authed user; updates bcrypt hash; rejects 401 on wrong current"
    - "PUT /usuarios/{id}/password admin-only; updates bcrypt hash; no current_password required"
    - "Every CRUD op on Usuarios auto-emits an audit_log row with password_hash=None in the JSON snapshot (D-26 redaction already wired)"
---

<!-- Revision 2 (plan-checker iteration 2 of 3, max):
  - Fixed BLOCKER 1: tours_servicios has NO activo column (verified at models/tours.py:111-124 + routers/tours.py:389). Replaced universal `text(f"{tabla}.{columna} = :id AND {tabla}.activo = 1")` predicate with per-entry `uses_activo` flag — all 5 catalog entities' check was hitting this bug since tours_servicios is the FK target for every one.
  - Fixed BLOCKER 2: tours_servicios has `moneda` (Enum MonedaCodigo), NOT `moneda_id` (verified at models/tours.py:118). Removed `("tours_servicios", "moneda_id")` wrong entry; added `tours_servicios.moneda` with `bind_name="val"` (matches the Monedas row's codigo "PEN"/"USD", not the integer id).
  - Fixed BLOCKER 3: special-case `columna == "moneda" and entidad == "monedas"` branch was hardcoded to query `cuentas` only. Eliminated entirely — every entry is now self-describing via `(tabla, columna, bind_name, uses_activo)` tuple, so the same code path handles every ref uniformly.
  - Added `comision_reglas` to `vendedores` and `tours` references (comision_reglas.vendedor_id and .tour_id are real FKs, with `activo=1` filter — comision_reglas has its own activo column).
  - Added `liquidaciones` to `agencias` and `vendedores` references (liquidaciones.agencia_id and .vendedor_id are nullable FKs; no activo filter — liquidaciones has no activo column).
  - Renamed `test_delete_catalogo_sin_referencias_retorna_204` → `test_delete_catalogo_sin_referencias_retorna_200` and "200/204" → "200" (the existing handler returns 200, not 204 — Ponytail: keep the 200, don't introduce 204 complexity).
  - Fixed Flag #4: `autonomous: true` → `false` to match Plan 02 + Phase 02.1 pattern. (No checkpoints in this plan; autonomous=false keeps the project convention without a hard requirement.)
  - All 9 test behaviors in the <behavior> block are still achievable; test code shape is unchanged, only the data structure behind the implementation changes.
-->

<objective>
Complete the backend CRUD surface for Phase 02.1.1: PUT catalogos (codigo/nombre, activo preserved), referential-integrity check on DELETE catalogos (409 with referencias list), and the full /usuarios router (GET list, POST create, PUT edit, DELETE soft) plus the two password endpoints (self-service + admin reset), with all RBAC, last-admin, self-delete, email-unique, and password-hash redaction guards in place.

Purpose: Frontend in Plan 02 needs the backend verbs + guards before it can wire modals, the /perfil page, and the real /admin/usuarios page. Audit log is the structural enforcement per Phase 02.1 D-21/D-22/D-26 — every write to `usuarios` must leave a `password_hash=null` row in audit_log.

Output: routers/core.py extended (PUT catalogos + ref check + RBAC switch to admin+contabilidad for catalog CRUD); routers/usuarios.py (new — full CRUD + 2 password endpoints); schemas/core.py (5 new Pydantic models); main.py (register new router); tests/test_catalogos.py + tests/test_usuarios.py (TDD — RED then GREEN, matching the project convention from 02.1-02).
</objective>

<execution_context>
@/home/luciel/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/luciel/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1.1-tours-crud-cat-logos-gesti-n-usuarios/02.1.1-CONTEXT.md
@.planning/phases/02.1.1-tours-crud-cat-logos-gesti-n-usuarios/02.1.1-PATTERNS.md
@.planning/phases/02.1-tours-panel-contable-hotel/02.1-01-SUMMARY.md

# Backend code to extend (read these before editing)
@apps/tours/api/app/routers/core.py
@apps/tours/api/app/routers/tours.py
@apps/tours/api/app/routers/auth.py
@apps/tours/api/app/dependencies.py
@apps/tours/api/app/schemas/core.py
@apps/tours/api/app/schemas/tours.py
@apps/tours/api/app/models/core.py
@apps/tours/api/app/models/tours.py
@apps/tours/api/app/audit.py
@apps/tours/api/app/main.py
@apps/tours/api/app/seed.py
@apps/tours/api/app/config.py
@apps/tours/api/tests/conftest.py
@apps/tours/api/tests/test_rbac.py
@apps/tours/api/tests/test_audit_log.py
@apps/tours/api/tests/test_ventas.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PUT /catalogos/{entidad}/{id} + DELETE referential check + RBAC switch to admin+contabilidad</name>
  <read_first>
    - apps/tours/api/app/routers/core.py (existing catalogos POST/DELETE at lines 122-154; PUT /cuentas mold at lines 64-80)
    - apps/tours/api/app/models/tours.py (ToursServicios FK columns at lines 111-124; cuentas.moneda is enum column — special case for `monedas` ref check)
    - apps/tours/api/app/dependencies.py (require_role at lines 45-51 — already variadic *roles, no code change)
    - apps/tours/api/tests/test_rbac.py (JWT minting helper `_token` at lines 20-31 — reuse as-is)
  </read_first>
  <files>
    apps/tours/api/app/routers/core.py
    apps/tours/api/tests/test_catalogos.py
  </files>
  <behavior>
    - test_put_catalogo_updates_codigo_nombre: PUT /catalogos/agencias/1 with {codigo, nombre} as admin → 200, response shows updated values, activo unchanged
    - test_put_catalogo_preserva_activo: PUT /catalogos/agencias/1 with body that does not mention activo → row's activo column is unchanged
    - test_put_catalogo_contabilidad_acepta: PUT /catalogos/agencias/1 as contabilidad role → 200
    - test_put_catalogo_vendedor_rechazado: PUT /catalogos/agencias/1 as vendedor role → 403
    - test_delete_catalogo_en_uso_retorna_409: Insert a tours_servicios row directly via `async_session` (faster than POST /ventas; no liquidaciones) referencing seeded agencia id=1, then DELETE /catalogos/agencias/1 as admin → 409 with detail.mensaje containing "referencian" and detail.referencias = [{tabla: "tours_servicios", count: 1}] (liquidaciones count is 0 — no liquidaciones exist in this test, so the only ref is tours_servicios)
    - test_delete_catalogo_sin_referencias_retorna_200: DELETE /catalogos/agencias/1 as admin when no tours_servicios/comision_reglas/liquidaciones reference it → 200 with `{"ok": true}` body, row.activo=false in DB
    - test_delete_catalogo_contabilidad_acepta: DELETE /catalogos/agencias/{id} (sin refs) as contabilidad → 200
    - test_delete_catalogo_vendedor_rechazado: DELETE /catalogos/agencias/{id} as vendedor → 403
    - test_delete_catalogo_monedas_bloqueado_si_hay_cuentas: With seeded moneda PEN (id=1, codigo="PEN") and seeded cuenta 101-CAJA-PEN (id=1, moneda=PEN, activo=true) — DELETE /catalogos/monedas/1 as admin → 409 with detail.mensaje containing "referencian" and detail.referencias including {tabla: "cuentas", count: ≥1} (and NO `row.activo` assertion — Monedas has no `activo` column; the 409 short-circuits before any soft-delete attempt)
  </behavior>
  <action>
    Write the tests in `apps/tours/api/tests/test_catalogos.py` FIRST (RED) and run them — they MUST fail because PUT catalogos does not exist and DELETE has no referential check. Use `_token(role, user_id)` helper from `test_rbac.py` pattern; mint admin/contabilidad/vendedor JWTs as needed. For the `test_delete_catalogo_en_uso_retorna_409` test, insert a `ToursServicios` row directly via the `async_session` fixture (faster than POST /ventas, no liquidaciones side-effects) referencing seeded agencia id=1. `seed.py` does NOT create an `Asientos` row (only chart of cuentas, admin user, comision regla, and 1 row each of catalogos), so first create a minimal `Asientos(fecha=date(2024,1,1), concepto="test-fixture")`, flush to get its id, then insert `ToursServicios(agencia_id=1, vendedor_id=1, tour_id=1, forma_pago_id=1, moneda=MonedaCodigo.PEN, monto=100, fecha=date(2024,1,1), asiento_id=<asiento.id>)`. The resulting ref check on DELETE /catalogos/agencias/1 must find exactly 1 tours_servicios row and 0 liquidaciones rows, yielding `detail.referencias == [{"tabla": "tours_servicios", "count": 1}]`. Alternative: call POST /ventas as admin via the `client` fixture — slower, exercises more code, but also valid. Pick the direct-insert approach (Ponytail: one fixture row, no extra /ventas dependency).

    Then implement (GREEN):

    1. In `routers/core.py`:
       - Add `PUT /catalogos/{entidad}/{row_id}` after the existing `POST /catalogos/{entidad}` (line 122). Follow the exact shape of `update_cuenta` at lines 64-80. RBAC: `Depends(require_role("admin", "contabilidad"))` (D-13). Mutation: assign `body.codigo` to `row.codigo` and `body.nombre` to `row.nombre`. Do NOT touch `row.activo` (D-03). Call `await session.commit(); await session.refresh(row); return row`. Return type `CatalogoOut`.
       - Add referential check inside the existing `DELETE /catalogos/{entidad}/{row_id}` (line 139). The check structure is module-level `_REFERENCED_BY: dict[str, list[tuple[str, str, str, bool]]]` mapping each catalog entity to a list of `(tabla, columna, bind_name, uses_activo)` tuples per D-19, derived from the actual models (NOT a hardcoded predicate — see derivation below). The four fields per tuple:
         - `tabla: str` — the table that has the FK (e.g. `tours_servicios`, `comision_reglas`, `cuentas`, `liquidaciones`).
         - `columna: str` — the FK column name (e.g. `agencia_id`, `moneda`).
         - `bind_name: str` — either `"id"` (binds `:id = row_id`) or `"val"` (binds `:val = row.codigo`); the `"val"` form is for the enum-by-codigo case where the FK column stores the codigo string ("PEN"/"USD") instead of the integer id.
         - `uses_activo: bool` — `True` if the predicate also filters `<tabla>.activo = 1`; `False` otherwise.
       - Derivation (verified by reading `models/tours.py`, `models/core.py`, and the FK definitions):
         - `agencias` → `[("tours_servicios", "agencia_id", "id", False), ("liquidaciones", "agencia_id", "id", False)]`
         - `vendedores` → `[("tours_servicios", "vendedor_id", "id", False), ("comision_reglas", "vendedor_id", "id", True), ("liquidaciones", "vendedor_id", "id", False)]`
         - `tours` → `[("tours_servicios", "tour_id", "id", False), ("comision_reglas", "tour_id", "id", True)]`
         - `formas-pago` → `[("tours_servicios", "forma_pago_id", "id", False)]`
         - `monedas` → `[("tours_servicios", "moneda", "val", False), ("cuentas", "moneda", "val", True)]`
       - Why these flags: `tours_servicios` has no `activo` column (confirmed at `models/tours.py:111-124` and the explicit comment at `routers/tours.py:389`: "Soft-delete via activo=0 on tours_servicios is not applicable") — so all its refs have `uses_activo=False`. `liquidaciones` also has no `activo` column (`models/tours.py:97-108`) — so its refs have `uses_activo=False`. `comision_reglas` and `cuentas` have `activo` columns — so their refs have `uses_activo=True`. The `moneda` column in both `tours_servicios` (`models/tours.py:118`) and `cuentas` (`models/core.py:89`) is `Mapped[MonedaCodigo] = mapped_column(Enum(MonedaCodigo), ...)`, NOT a FK to `monedas.id` — so `bind_name="val"` and the bound value is the `Monedas` row's `codigo` string ("PEN" or "USD"). No entry for `moneda_id` exists anywhere: that column does not exist on any referencing table.
       - The check loop (single code path, no special cases): for each entity, look up `_REFERENCED_BY.get(entidad, [])` (empty list if entity has no refs — proceed directly to soft-delete). Iterate the tuples. For each `(tabla, columna, bind_name, uses_activo)`, build the bind dict based on `bind_name`: `{"id": row_id}` if `"id"`, `{"val": row.codigo}` if `"val"`. Build the SQL: start with `f"{tabla}.{columna} = :{bind_name}"`, and if `uses_activo` is True, append `f" AND {tabla}.activo = 1"`. Wrap in `text(...).bindparams(**bind_dict)`. Count via `select(func.count()).select_from(text(tabla)).where(predicate)` and read `.scalar_one()`. If `count > 0`, append `{"tabla": tabla, "count": int(count)}` to a `refs: list[dict]`. After the loop, if `refs` is non-empty, raise `HTTPException(409, detail={"mensaje": f"No se puede desactivar — {sum(r['count'] for r in refs)} registros lo referencian", "referencias": refs})`. This shape is what the frontend reads at `err.detail.mensaje` + `err.detail.referencias` for the toast (D-21).
       - Place the check AFTER the existing `if row is None: raise 404` and BEFORE the `row.activo = False` line — so a successful 409 path never reaches `row.activo = False` (which would crash for `Monedas` anyway since it has no `activo` column; the 409 short-circuit is the only safe path for monedas DELETE). For catalogos entities with NO entries in `_REFERENCED_BY` (none today, but defensive), the loop is a no-op and the soft-delete proceeds — same as before this plan.
       - Change the RBAC on existing `DELETE /catalogos/{entidad}/{row_id}` (line 144) and existing `POST /catalogos/{entidad}` (line 127) from `require_role("admin")` to `require_role("admin", "contabilidad")` (D-13). Do NOT change `/cuentas` (admin-only — admin manages chart of accounts) and do NOT change `/audit-log` (D-14 admin-only preserved).
       - Update the import line at top of `routers/core.py`: change `from sqlalchemy import select` → `from sqlalchemy import func, select, text` (add `func` + `text` for the new referential check).

    2. Verify after the run: `cd apps/tours/api && pytest tests/test_catalogos.py -x` → all green. Then `pytest tests/ -x` → all 32 prior tests still pass (the RBAC change for catalogos does not affect other suites — `test_rbac.py::test_post_agencia_vendedor_returns_403` still expects 403 on POST /catalogos/agencias from vendedor, which holds).
  </action>
  <verify>
    <automated>cd apps/tours/api && pytest tests/test_catalogos.py -x && pytest tests/ -x</automated>
  </verify>
  <done>
    - All 9 tests in `test_catalogos.py` pass
    - All 32 prior tests still pass (regression intact)
    - PUT /catalogos/{entidad}/{id} updates codigo/nombre only (activo untouched, verified by re-reading row after PUT)
    - DELETE /catalogos/{entidad}/{id} with FK references returns 409 + `{"detail": {"mensaje": "...", "referencias": [{"tabla": ..., "count": N}, ...]}}`
    - DELETE /catalogos/monedas/{PEN} returns 409 referencing both `tours_servicios` and `cuentas`
    - Vendedor role gets 403 on POST/PUT/DELETE catalogos
    - Contabilidad role gets 200 on POST/PUT/DELETE catalogos
    - Commit message: `feat(02.1.1-01): catalogos PUT + DELETE ref check + RBAC admin+contabilidad` (with prior `test(02.1.1-01): ...` RED commit per project TDD convention from 02.1-02)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /usuarios router (GET/POST/PUT/DELETE + me/password + {id}/password) with all guards + audit redaction</name>
  <read_first>
    - apps/tours/api/app/routers/auth.py (bcrypt.checkpw pattern at line 25 — reuse for current_password verification)
    - apps/tours/api/app/seed.py line 46 (bcrypt.hashpw with `settings.BCRYPT_COST` rounds — reuse for new password hashing)
    - apps/tours/api/app/models/core.py lines 69-80 (Usuarios model with __sensitive_fields__={"password_hash"} — D-26 redaction already wired; password_hash column is `String(128)`)
    - apps/tours/api/app/audit.py lines 40-47 (`_snapshot` uses mapper.column_attrs, handles metadata_ aliasing — used by before_flush automatically for Usuarios writes)
    - apps/tours/api/app/routers/tours.py (existing routers show the response_model pattern + session.commit() + session.refresh() convention)
  </read_first>
  <files>
    apps/tours/api/app/routers/usuarios.py
    apps/tours/api/app/schemas/core.py
    apps/tours/api/app/main.py
    apps/tours/api/tests/test_usuarios.py
  </files>
  <behavior>
    - test_post_usuario_creates_with_bcrypt_hash: POST /usuarios as admin with {email, username, password, rol} → 201; response has id/email/username/rol/activo (no password_hash); DB row has password_hash starting with $2 (bcrypt prefix); audit_log has 'I' row on usuarios tabla with password_hash=null
    - test_post_usuario_duplicate_email_409: POST /usuarios twice with same email → second returns 409
    - test_post_usuario_vendedor_403: POST /usuarios as vendedor → 403
    - test_get_usuarios_excludes_password_hash: GET /usuarios as admin → 200, list of dicts each with id/email/username/rol/activo/creado_en/ultimo_acceso, no password_hash key in any response
    - test_put_usuario_updates_fields: PUT /usuarios/{id} as admin with {email, username, rol, activo} → 200; DB row reflects changes
    - test_put_usuario_no_password: PUT /usuarios/{id} with {password: "x"} → 400 (schema rejects unknown field per Pydantic model_extra forbid) OR password is silently ignored (planner choice: use Pydantic `model_config = ConfigDict(extra="forbid")` on UsuarioUpdateIn so an attempted password change is a 422 — fail loud per D-06)
    - test_put_usuario_vendedor_403: PUT /usuarios/{id} as vendedor → 403
    - test_put_usuario_last_admin_role_change_409: seed 1 admin, PUT /usuarios/{admin_id} with {rol: "vendedor"} → 409
    - test_delete_usuario_soft: DELETE /usuarios/{id} as admin (other user) → 200; DB row.activo=false; audit_log has 'U' row (soft-delete is an UPDATE)
    - test_delete_usuario_self_409: DELETE /usuarios/{current_admin_id} as admin → 409 with detail containing "propia cuenta"
    - test_delete_usuario_last_admin_409: only 1 admin in DB, DELETE that admin → 409
    - test_delete_usuario_vendedor_403: DELETE /usuarios/{id} as vendedor → 403
    - test_put_me_password_success: PUT /usuarios/me/password with {current_password: "change-me", new_password: "new-secret"} as admin → 200; DB row's password_hash changes; old hash no longer validates with bcrypt.checkpw; new hash validates
    - test_put_me_password_wrong_current_401: PUT /usuarios/me/password with {current_password: "wrong", new_password: "new-secret"} → 401; password unchanged
    - test_put_me_password_audit_redaction: after PUT /usuarios/me/password, audit_log has 'U' row for usuarios where password_hash=null in datos_antes AND datos_despues (D-26)
    - test_put_admin_password_reset: PUT /usuarios/{other_id}/password as admin with {new_password: "x"} → 200; DB row's password_hash changes; no current_password required
    - test_put_admin_password_non_admin_403: PUT /usuarios/{other_id}/password as vendedor → 403
  </behavior>
  <action>
    Write tests FIRST (RED) in `apps/tours/api/tests/test_usuarios.py`. Use `_token` helper from `test_rbac.py` pattern. Use the `client` fixture (in-memory DB + admin seeded by conftest). For "current user" tests, mint a JWT with `sub="1"` (matches the seeded admin id=1).

    Then implement (GREEN):

    1. In `apps/tours/api/app/schemas/core.py`, append 5 new Pydantic models at the bottom (do not touch existing schemas — phase 02.1-02 schemas stay):
       - `UsuarioCreateIn` — fields: `email: str`, `username: str` (min 3 chars via `Field(min_length=3)`), `password: str` (min 8 via `Field(min_length=8)`), `rol: Rol`. No `activo` (defaults to True in model). No `ultimo_acceso`.
       - `UsuarioUpdateIn` — fields: `email: str | None = None`, `username: str | None = None`, `rol: Rol | None = None`, `activo: bool | None = None`. NO `password` field (D-06). Use `model_config = ConfigDict(extra="forbid")` so any client sending `password` here gets 422.
       - `PasswordChangeIn` — fields: `current_password: str`, `new_password: str` (min 8).
       - `AdminPasswordResetIn` — fields: `new_password: str` (min 8).
       - `UsuarioOut` — `model_config = ConfigDict(from_attributes=True)`, fields: `id: int`, `email: str`, `username: str`, `rol: Rol`, `activo: bool`, `creado_en: datetime`, `ultimo_acceso: datetime | None`. No `password_hash` (D-26 — even if the ORM has it, Pydantic won't serialize).
       - Import `Field` from pydantic and `Rol` from `app.models.core` at the top of the file (Rol needs to be imported — model enum). Use `from pydantic import BaseModel, ConfigDict, Field` and `from app.models.core import Rol` (or import inside the model to avoid circular — Rol is in core.py, schemas is also in core.py; same file so import works).

    2. Create `apps/tours/api/app/routers/usuarios.py` (new file) with this shape:
       - Router `router = APIRouter(prefix="/usuarios", tags=["usuarios"])` (or no prefix; the routes below already include /usuarios). Use the same import block as `routers/core.py` plus `from sqlalchemy.exc import IntegrityError` and `import bcrypt` and `from app.config import settings`.
       - Helper `_assert_admin_remaining(session, exclude_user_id: int) -> None`: count `Usuarios WHERE rol==Rol.admin AND activo==True AND id != exclude_user_id`. If 0, raise `HTTPException(409, "No se puede — quedaría sin administradores activos")`. Inline helper, no module extraction (YAGNI; used 2x in this router).
       - `GET /usuarios` — admin or contabilidad (read-only listing). Actually D-15 says "CRUD usuarios sigue admin-only" — so this should be admin only. Planner: admin only (D-15). RBAC: `require_role("admin")`. Returns list of `UsuarioOut` from all `Usuarios` (no pagination per D-05 — YAGNI at this scale).
       - `POST /usuarios` — admin only. RBAC: `require_role("admin")`. Body: `UsuarioCreateIn`. Hash password with `bcrypt.hashpw(body.password.encode(), bcrypt.gensalt(rounds=settings.BCRYPT_COST)).decode()`. Wrap `session.commit()` in try/except `IntegrityError`; if the error message contains "usuarios.email", rollback and raise `HTTPException(409, "Email ya registrado")`. Otherwise re-raise. Set `activo=True` explicitly.
       - `PUT /usuarios/{usuario_id}` — admin only. RBAC: `require_role("admin")`. Body: `UsuarioUpdateIn`. Fetch user; if None, 404. If `body.rol is not None` and `body.rol != Rol.admin` and the target user is currently admin → call `_assert_admin_remaining(session, exclude_user_id=usuario_id)`. Apply non-None fields. `await session.commit(); await session.refresh(user); return user`.
       - `DELETE /usuarios/{usuario_id}` — admin only. RBAC: `require_role("admin")`. Fetch user. If `usuario_id == int(current_user["id"])`, raise `HTTPException(409, "No puede eliminar su propia cuenta")` (D-12). If user is admin, call `_assert_admin_remaining(session, exclude_user_id=usuario_id)` (D-11). Soft-delete: `user.activo = False; await session.commit()`. Return `{"ok": True}`. (Audit listener will write a 'U' row because soft-delete is an UPDATE — this is the expected behavior; D-22 + D-26 already handle the snapshot + redaction.)
       - `PUT /usuarios/me/password` — any authed user. RBAC: `get_current_user` (no role gate). Fetch the current user from DB using `int(user["id"])`. Verify `body.current_password` with `bcrypt.checkpw(body.current_password.encode(), user.password_hash.encode())` — if False, 401. Hash `body.new_password` and assign to `user.password_hash`. `await session.commit()`. Return `{"ok": True}`. Audit listener captures the 'U' write with password_hash=null in both snapshots (D-26).
       - `PUT /usuarios/{usuario_id}/password` — admin only. RBAC: `require_role("admin")`. Body: `AdminPasswordResetIn`. Fetch target user. Hash new password, assign, commit. Return `{"ok": True}`.

    3. In `apps/tours/api/app/main.py`, register the new router: add `from app.routers import usuarios as usuarios_router  # noqa: E402` after the existing imports and `app.include_router(usuarios_router.router)` after the existing `app.include_router(tours_router.router)`. Do not change any other line.

    4. Verify after: `cd apps/tours/api && pytest tests/test_usuarios.py -x && pytest tests/ -x`. All 17 new tests pass; all 32 prior tests still pass. `pnpm build` in `apps/tours/web` is NOT needed (frontend untouched). `docker compose config --quiet` also not needed (no compose changes).
  </action>
  <verify>
    <automated>cd apps/tours/api && pytest tests/test_usuarios.py -x && pytest tests/ -x</automated>
  </verify>
  <done>
    - All 17 tests in `test_usuarios.py` pass
    - All 32 prior tests still pass (regression intact — test_audit_log redaction test on Usuarios INSERT still works)
    - GET /usuarios returns list of users with no password_hash field in any response
    - POST /usuarios creates a user; the audit_log has an 'I' row for `usuarios` with password_hash=null in datos_despues
    - POST /usuarios with duplicate email returns 409
    - PUT /usuarios/{id} does NOT accept a `password` field (422 from Pydantic)
    - PUT /usuarios/{id} with role change away from admin when target is last admin → 409
    - DELETE /usuarios/{id} on self → 409; on last admin → 409; on other admin → 200, activo=false, audit 'U' row
    - PUT /usuarios/me/password with correct current → 200, DB hash changes, audit 'U' row with password_hash=null in both snapshots
    - PUT /usuarios/me/password with wrong current → 401, hash unchanged
    - PUT /usuarios/{id}/password as admin → 200, hash changes, no current_password needed
    - PUT /usuarios/{id}/password as vendedor → 403
    - Commit message: `feat(02.1.1-01): /usuarios router (CRUD + 2 password endpoints) with audit redaction` (with prior `test(02.1.1-01): ...` RED commit per project TDD convention)
  </done>
</task>

</tasks>

<verification>
- `cd apps/tours/api && pytest tests/ -x` exits 0 with all 49 tests green (32 prior + 9 catalogos + 17 usuarios — note some prior tests overlap so actual count may be slightly different; the gate is "all pass").
- `python3 -c "import yaml; yaml.safe_load(open('docker-compose.yml'))"` exits 0 (compose untouched but ensure no accidental edits).
- `grep -nE 'require_role\("admin"\)' apps/tours/api/app/routers/core.py` returns ONLY the `/cuentas` and `/audit-log` lines (3 calls in core.py: 2 cuenta endpoints + audit-log) — confirm no catalogos endpoint still uses `require_role("admin")` alone.
- `grep -nE '_REFERENCED_BY' apps/tours/api/app/routers/core.py` returns the module-level dict definition.
- `grep -nE 'def (list|create|update|delete|change_password|admin_reset_password)_?usuarios?' apps/tours/api/app/routers/usuarios.py` returns 6 handler functions.
- `grep -nE 'app.include_router' apps/tours/api/app/main.py` returns 4 lines (auth, core, tours, usuarios).
- `pytest tests/test_usuarios.py -k audit_redaction -v` exits 0 confirming D-26 redaction in the password-change audit snapshot.
</verification>

<success_criteria>
Phase 02.1.1 backend complete when:
1. `curl -X PUT http://localhost:8000/catalogos/agencias/1 -H "Authorization: Bearer <admin-or-contabilidad-jwt>" -H "Content-Type: application/json" -d '{"codigo":"AG-001","nombre":"Agencia demo renamed"}'` returns 200 with the updated row (activo unchanged).
2. `curl -X DELETE http://localhost:8000/catalogos/agencias/1 -H "Authorization: Bearer <admin-or-contabilidad-jwt>"` with at least one tours_servicios referencing id=1 returns 409 + `{"detail":{"mensaje":"...","referencias":[{"tabla":"tours_servicios","count":N}]}}`.
3. `curl -X POST http://localhost:8000/usuarios -H "Authorization: Bearer <admin-jwt>" -H "Content-Type: application/json" -d '{"email":"vendedor2@x.com","username":"vendedor2","password":"longenough","rol":"vendedor"}'` returns 201 with the new user (no password_hash field).
4. `curl -X POST http://localhost:8000/usuarios -d '{"email":"vendedor2@x.com",...}'` (same email) returns 409.
5. `curl -X DELETE http://localhost:8000/usuarios/1 -H "Authorization: Bearer <admin-jwt>"` (deleting self) returns 409.
6. `curl -X PUT http://localhost:8000/usuarios/me/password -H "Authorization: Bearer <admin-jwt>" -H "Content-Type: application/json" -d '{"current_password":"change-me","new_password":"newpass123"}'` returns 200 and the audit_log has a 'U' row for usuarios with password_hash=null in both snapshots.
7. `curl http://localhost:8000/usuarios -H "Authorization: Bearer <vendedor-jwt>"` returns 403.
8. `curl http://localhost:8000/usuarios -H "Authorization: Bearer <contabilidad-jwt>"` returns 403 (CRUD usuarios is admin-only per D-15).
</success_criteria>

<output>
Create `.planning/phases/02.1.1-tours-crud-cat-logos-gesti-n-usuarios/02.1.1-01-SUMMARY.md` when done
</output>
