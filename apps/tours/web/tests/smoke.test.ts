import { describe, it, expect } from "vitest";

// Wave 0 smoke — real component tests deferred to Plan 02.1-02 per plan acceptance criteria.
describe("smoke", () => {
  it("passes", () => {
    expect(true).toBe(true);
  });
});